# 경희 통합 공지·연락처 백엔드 작업 계획

작성일: 2026-09-06. 상태: 백엔드 구현 전 계획안. 이번 작업에서는 계획 문서만 작성하며 서버·데이터베이스·수집기를 생성하지 않는다. 별도로 진행되는 프론트 작업의 상태와는 무관하다.

제품 요구사항은 [기존 기획](KHU_INTEGRATED_NOTICE_PLAN.md), 프론트 가상 데이터 제작 기준은 [화면 연동 규격 초안](KHU_API_CONTRACT_DRAFT.md)에 둔다. 프론트의 화면·컴포넌트·배치는 별도 작업이다. 이 문서의 처리량·일정·비용 상한은 계획 가정이며 실측 결과가 아니다.

2026-09-06 개정: 상시 서버(Lightsail)와 유료 관리형 데이터베이스(Supabase Pro)를 전제로 한 초안(월 52~70달러)을 폐기하고, **상시 서버 없이 월 0원**으로 운영하는 MVP(최소 기능 제품) 구성으로 바꿨다. 근거는 16절과 [GitHub Actions 실행 서버에서 경희대 게시판 3곳 접근 검증](https://github.com/sihort183-creator/khu/actions/runs/33980111146)이다. 학교 지원이 확정되면 19절의 순서로 유료 자원을 추가한다.

## 1. 이번 계획의 결정 사항

| 항목 | 기본안 | 이유 |
|---|---|---|
| 제품 데이터 | 공지와 연락처를 독립 분류로 제공 | 상시 연락처에는 공지의 게시일·마감·중복 묶기 규칙을 강제하지 않음 |
| 서버 형태 | 상시 서버 없음. 수집·정규화·중복 처리·정적 파일 생성을 GitHub Actions(저장소 자동 실행 서비스)의 예약 실행 1시간 주기로 한 번에 수행 | 서버 비용·운영 체제 관리 0. 공개 저장소는 실행 시간 무제한 무료 |
| 서버 언어 | Python(파이썬) | 웹 수집, 문자열 처리, 추후 이미지 문자 인식을 한 언어로 관리 |
| 조회 제공 | 정적 JSON 파일을 R2에 두고 Cloudflare Worker(무료 요청 처리기)가 캐시·CORS를 붙여 제공. 검색·신고·개인 기능은 Supabase의 자동 REST(PostgREST) 사용 | 사용자 조회에 서버 프로세스가 없음. 응답 모델은 Pydantic(입출력 검증 도구)으로 생성 시점에 검증 |
| 데이터베이스 | Supabase(관리형 PostgreSQL·인증 서비스) 무료 요금제 1개 프로젝트, 500MB | 원본·연결 관계·수집 상태를 트랜잭션으로 관리. 매시간 쓰기가 발생해 무료 프로젝트의 1주 미사용 정지에 걸리지 않음. 투자 후 Pro(월 25달러)로 올리면 코드 변경 없음 |
| 저장소 접근 | SQLAlchemy(데이터베이스 접근 도구) + Alembic(스키마 변경 관리 도구) | 저장 규칙과 변경 이력을 코드로 관리 |
| 작업 실행 단위 | 1회 실행(run) 안에서 출처를 순서대로 처리. 대기열 없음. 출처별 마지막 성공·실패·다음 시도 시각만 데이터베이스에 저장 | 실행 간 겹침은 GitHub Actions의 동시 실행 그룹으로 차단. 재시도는 다음 실행에서 |
| 수집 도구 | HTTPX(웹 요청 도구) + lxml(HTML 구조 분석 도구) | 정적 페이지 우선, 브라우저 실행은 필요한 출처만 추가 |
| 검색·중복 후보 | pg_trgm(문자열 유사 검색 확장) + 규칙 기반 비교 | 유료 인공지능 호출 없이 시작 |
| 파일 보관·정적 제공 | Cloudflare R2(파일 저장 서비스) 무료 구간(월 10GB). 원문 증거·백업은 비공개 버킷, 조회용 JSON은 Worker 전용 버킷 | 외부 전송 요금 없음. 사용자가 늘어도 조회 비용 0 |
| 운영 서버 | 없음. 필요해지면(19절) 소형 정액 서버에 같은 파이썬 코드를 cron(예약 실행)으로 올림 | 수집 지연 허용 범위가 1시간이라 예약 실행 지연(최대 30~60분)이 문제 되지 않음 |
| 배포 | 저장소 `main` 병합이 곧 배포. 스키마 변경은 Alembic을 Actions에서 실행 | 배포 서버·이미지 저장소 없음 |
| 운영 중 인공지능 비용 | 언어 모델·외부 임베딩 호출 0회 | 기존 제품 원칙 유지 |

프레임워크별 정확한 버전은 첫 구현에서 지원 중인 안정 버전과 운영 데이터베이스 호환성을 확인해 잠금 파일로 고정한다. 이 계획을 이유로 무조건 최신 주 버전으로 올리지 않는다. Pydantic 모델에서 JSON Schema(자료 형식 정의)를 생성해 정적 파일과 프론트 가상 자료를 같은 규격으로 검증한다.

초기에는 상시 서버, 별도 검색 서버, Redis(메모리 저장 서버), 컨테이너 운영 도구, 여러 독립 서비스로의 분할을 도입하지 않는다. 도입 기준은 19절에 둔다. 웹 수집은 HTTPX+lxml로만 하며 브라우저 실행(Playwright 등)은 Actions 실행 시간 안에서 감당 가능한 출처에만 4차에서 검토한다.

## 2. 범위와 출시 단위

| 출시 단위 | 포함 | 완료 의미 |
|---|---|---|
| 1차 웹 공개 | 조직·출처 등록, 웹 수집, 기본 중복 처리, 공지 조회·검색, 연락처 조회·검색, 비회원 맞춤 조회, 운영 기능·백업 | 명시한 출처 범위에서 실제 원문과 응답이 일치하고 누락·장애 상태를 공개할 수 있음 |
| 2차 계정 동기화 | 선택적 로그인, 학과·구독 저장, 저장한 공지, 탈퇴·데이터 삭제 | 다른 기기에서도 설정 복원, 다른 사용자 데이터 접근 차단 |
| 3차 인스타그램 | 가능한 공식 연동과 기관 제공 자료, 별도 수집 워크플로, 출처별 연결 상태 | 실제 접근 가능한 계정만 수집 완료로 집계 |
| 4차 품질 확장 | 유사 중복 고도화, 첨부·이미지 비교, 필요시 로컬 문자 인식·마감 추출 | 검증 자료에서 오탐·누락 기준 통과 후 단계적 활성화 |

**MVP(1차 웹 공개)에서 인스타그램은 제외한다.** 2026-09-06 결정. 근거: 학생회 계정 대부분이 개인 계정이라 공식 API(business_discovery)로 조회할 수 없고, 프로페셔널 계정 전환 협조는 MVP 기간 안에 기대하기 어렵다. 스크래핑(브라우저 자동화 포함)은 약관 위반과 계정 정지 위험 때문에 채택하지 않는다. 인스타그램은 전체 제품 범위에는 남기되 3차 이후로 미루며, MVP 화면에서는 출처 상태 `pending`(연결 대기)으로만 노출하고 수집 중인 것처럼 표시하지 않는다. 재개 조건은 11.1절에 둔다.

프론트 구현, 광고 관리 서버, 결제, 학교 포털 로그인, 학번·재학 인증, 이메일·문자·앱 푸시 발송은 이번 백엔드 1차 범위에 포함하지 않는다. 학생회·학과에 연락하거나 메일을 보내는 작업도 이 문서 작성으로 실행하지 않는다.

## 3. 실제 정본과 전체 데이터 흐름

```text
학교·기관의 공개 원문 / 공식 연락처 안내 / 연결된 공식 계정
  → 출처 등록·설정 버전 확정
  → 목록 탐색·상세 요청·원문 증거 보관
  → 추출 결과 검증·정규화·변경 이력 저장
  → 공지 분류·중복 묶기 / 연락처 항목과 근거 연결
  → PostgreSQL의 편집 정본과 사용자 조회용 데이터
  → 공통 조회 계층·응답 모델
  → 공개 조회의 짧은 캐시 / 개인 응답의 캐시 금지
  → 별도 프론트가 실제 응답을 표시
```

| 데이터 | 정본·소유자 | 다른 곳에 존재하는 사본 | 변경·삭제 시 처리 |
|---|---|---|---|
| 기관이 공표한 내용 | 원문 사이트·계정 | 비공개 원문 보관, 추출본 | 원문 변경을 새 이력으로 보존 |
| 조직·출처의 초기 조사 | 저장소의 초기 등록 파일 | 운영 데이터베이스의 최초 등록값 | 최초 가져오기 후 파일을 운영 정본처럼 매번 덮어쓰지 않음 |
| 운영 중 조직·출처 설정 | 데이터베이스의 버전 있는 설정, 운영자 변경 기능 | 내보내기 파일·감사 기록 | 이전 버전과 비교 후 새 버전 적용, 관련 작업 재생성 |
| 수집된 원문 게시물 | 원본 식별자와 변경 이력 | 검색문·화면용 본문 | 변환 버전으로 재생성 가능 |
| 운영자 보정 | 별도 보정 기록: 대상 필드·값·근거·작업자 | 화면용 최종값 | 다음 수집이 조용히 덮어쓰지 않도록 충돌 검토 |
| 공지 묶음·분류·검색문 | 원본·규칙·운영 보정에서 파생 | 조회 응답·캐시 | 재생성·병합·분리 후 참조와 캐시 함께 갱신 |
| 연락처 | 업무 단위 레코드 + 필드별 원문 근거 | 검색문·조회 응답 | 번호 교체와 검증 시각을 갱신, 충돌은 검토 상태 |
| 개인 설정·저장한 공지 | 사용자별 데이터베이스 레코드 | 비회원은 기기 내부 저장만 | 로그인 시 명시적 가져오기, 탈퇴 시 삭제 |

로컬은 로컬 데이터베이스, 검증 환경은 검증 데이터베이스, 운영은 운영 데이터베이스만 읽는다. 환경 혼용을 서버 시작 시 차단한다. 프론트의 가상 데이터는 어떤 환경에서도 운영 정본에 자동 저장하지 않는다.

## 4. 공통 불변 조건과 실패 판정

1. 동일 출처·동일 원본 식별자는 여러 번 수집해도 원본 게시물 1개다. 수정은 새 이력이다.
2. 공개되는 모든 공지와 연락처에는 원문 주소와 확인 시각이 있다. 자료가 없으면 지어내지 않는다.
3. 원문 내용·정규화 결과·운영 보정은 서로 구분된다. 재처리 결과를 원문으로 가장하지 않는다.
4. 출처의 소속 조직과 공지의 대상 범위는 별개다. 본부가 올렸다는 이유만으로 모든 글을 전교생 대상이라고 단정하지 않는다.
5. 연락처를 고쳤다고 공지 게시일이 바뀌거나 피드 최상단으로 올라오지 않는다.
6. 하나의 원본 게시물은 동시에 하나의 유효한 공지 묶음에 속한다. 병합·분리 이력은 되돌릴 수 있다.
7. 짧거나 빈 본문, 동일 양식 파일, 비슷한 제목, 같은 포스터 중 하나만으로 자동 병합하지 않는다.
8. 목록 읽기 실패·본문 분석 실패·접근 차단·진짜 빈 게시판을 구별한다. 분석 실패를 수집 성공 0건으로 기록하지 않는다.
9. 한 번의 누락·요청 실패로 기존 공지나 연락처를 삭제하지 않는다.
10. 인스타그램·이미지 처리·특정 출처 장애가 웹 조회와 다른 출처 수집을 멈추지 않는다.
11. 개인 설정은 서버에서 검증한 사용자에게만 귀속된다. 관리자 권한은 사용자가 보낸 값으로 결정하지 않는다.
12. 날짜만 알려진 원문에 임의의 마감 시각을 넣지 않는다. 원문 등록일·발견일·수정일을 분리한다.
13. 작업 재실행, 서버 재시작, 데이터베이스 복구 후에도 같은 입력은 같은 유효 레코드로 수렴한다.
14. 공개에서 숨긴 내용은 목록·상세·검색·연결 출처·연락처 참조·캐시에서 함께 사라진다.

위 조건은 데이터베이스 제약, 계약 검사, 원문 기반 회귀 검사, 장애 주입 검사로 구분해 구현한다. 기계적으로 검사 가능한 조건을 운영자 확인만으로 대체하지 않는다.

## 5. 출처 전수 조사와 등록 계획

### 5.1 조사 단위

기관 수, 도메인 수, 게시판 수, 연락처 안내 페이지 수, 인스타그램 계정 수를 각각 센다. 한 학과의 일반·장학 게시판은 같은 기관의 서로 다른 출처이며, 주소가 여러 개인 같은 게시판은 별칭으로 묶는다.

기존의 웹 180개 이상·전체 200개 이상은 설계용 추정치다. 전체 조사표의 분모가 확정되기 전에는 전체 수집률을 발표하지 않는다. 서울·국제 전체 조직을 조사하고, 공식 전화번호 안내에 존재하는 광릉도 캠퍼스 사전에 수용한다. 광릉의 실제 출시 포함 여부와 기관 범위는 조사표에 명시하며 누락시키지 않는다.

### 5.2 출처별 필수 조사 항목

| 묶음 | 저장할 내용 |
|---|---|
| 소속 | 기관 식별자, 기관 유형, 캠퍼스, 상위 조직, 별칭 |
| 콘텐츠 | 공지·연락처, 웹·인스타그램, 게시판별 주제, 대상 범위 기본값 |
| 접근 | 목록·상세 주소, 요청 방식·필수 매개변수, 페이지 이동, 문자 인코딩, 동적 표시 여부 |
| 원본 식별 | 게시물 번호, 안정 주소, 주소 별칭, 고정 공지 표시, 게시일·수정일 위치 |
| 연락처 특성 | 표·카드·하단 영역, 업무별 구분, 내선 규칙, 번호 범위, 전화·팩스 구분 |
| 운영 | 수집 주기, 같은 서버를 공유하는 묶음, 요청 간격, 시간 제한, 초기 수집 범위 |
| 검증 | 실제 원문 표본, 추출 기대값, 확인자·확인일, 공식 출처 근거 |
| 상태 | 후보·조사 중·검증됨·운영 중·보류·중지·폐쇄, 보류 사유 |
| 이용 조건 | 원문 사이트의 자동 수집·재게시·이메일 수집 관련 안내와 허용 범위 확인 기록 |

처음에는 서로 다른 게시판 구조의 10~15개 출처로 대표 표본을 만든다. 그 안에 양 캠퍼스, 본부, 단과대, 학과, 사업단, 독립 사이트, 연락처 표, 빈 목록, 첨부만 있는 공지를 포함한다. 이후 같은 구조의 전체 출처로 확장한다.

공통 게시판 형식이라는 가정도 원문으로 확인한다. 새 사이트를 등록할 때 공통 엔진에 도메인별 조건문을 늘리지 않고, 이미 지원하는 형식이면 설정만 추가한다. 새 형식이면 독립 어댑터와 원문 회귀 자료를 추가한다.

## 6. 데이터 모델과 제약

### 6.1 조직·출처

| 테이블 | 역할·주요 필드 | 필수 제약 |
|---|---|---|
| `universities`(대학) | 대학 식별자·명칭 | 대학 코드 유일 |
| `campuses`(캠퍼스) | 대학·캠퍼스 명칭 | 대학 안에서 코드 유일 |
| `organizations`(조직) | 상위 조직, 유형, 정식명·별칭, 운영 여부 | 순환 계층 금지, 같은 대학의 부모만 허용 |
| `organization_campuses`(조직의 캠퍼스 연결) | 공통 기관의 복수 캠퍼스 소속 | 중복 연결 금지 |
| `organization_closure`(조직의 조상·자손 관계) | 상속 조회용 파생 관계 | 원래 조직 계층에서 재생성 |
| `sources`(수집 출처) | 소속 조직, 매체, 콘텐츠 종류, 주소, 공식성 근거 | 안정 식별자 유지, 같은 주소·용도의 중복 등록 검토 |
| `source_config_versions`(출처 설정 이력) | 추출 설정, 주기·속도, 적용 버전·시각 | 활성 설정 1개, 이전 버전 불변 |
| `source_audiences`(출처 기본 대상) | 대학 전체·캠퍼스·조직·미확정 | 복수 대상 지원, 조직 소속과 별도 |
| `source_health`(출처 운영 상태) | 마지막 시도·목록 성공·상세 완료, 지연, 연속 실패, 누락 복구 상태 | 시도와 성공 시각 분리 |

### 6.2 원본·공지

| 테이블 | 역할·주요 필드 | 필수 제약 |
|---|---|---|
| `source_items`(원본 게시물) | 출처, 원본 번호, 안정 주소, 발견일, 현재 이력, 원문 상태 | 출처+원본 번호 유일. 번호 없는 경우 출처+정규화 주소 유일 |
| `source_item_aliases`(원문 주소 별칭) | 주소 변경·같은 글의 다른 주소 | 별칭이 둘 이상의 원본을 가리키면 검토 |
| `source_item_revisions`(원본 변경 이력) | 원문 저장 키, 제목·본문, 등록일·수정일, 추출 버전, 내용 해시 | 동일 내용 재요청 시 이력 중복 생성 금지 |
| `attachments`(첨부 정보) | 원문 링크·파일명·종류·크기·해시·확인 상태 | 파일 주소와 실제 파일 내용 식별 분리 |
| `notices`(사용자에게 보이는 공지 묶음) | 대표 원본, 상태, 최초 노출 시각, 분류·대상·검색 파생 버전 | 유효 대표 원본 필수 |
| `notice_sources`(공지와 원본 연결) | 공지 묶음, 원본, 연결·해제 사유 | 원본별 활성 연결 최대 1개 |
| `notice_audiences`(공지 대상 범위) | 조직·캠퍼스·전체 대상, 원문 근거, 규칙 버전 | 출처 기본값과 게시물 예외의 적용 순서 명시 |
| `notice_categories`(공지 주제) | 대표 주제와 보조 주제, 판정 근거 | 대표 주제 1개, 미분류는 기타 |
| `dedupe_decisions`(중복 판정 이력) | 후보 쌍, 사용 신호·점수, 자동·수동 판정, 규칙 버전 | 비교 당시 원본 이력 고정 |
| `notice_redirects`(병합 전 공지 주소 연결) | 이전 공지→현재 공지 | 순환 금지, 북마크·주소 연속성 유지 |
| `content_overrides`(운영 보정) | 필드별 보정값, 근거·작성자·유효기간 | 원문 이력 수정 금지 |

원본 게시물이 없는 기관 직접 등록도 운영자가 공식 제출 근거를 연결한 별도 출처의 원본 게시물로 받는다. 출처 없는 공지를 예외로 허용하지 않는다.

변경 이력 중복 방지는 현재 채택된 내용과 동일한 재요청에 적용한다. 내용이 A → B → A로 되돌아오면 마지막 A도 새로운 변경 사건으로 기록한다. 원문 파일은 내용 해시로 재사용할 수 있지만 게시물의 변경 순서를 없애지 않는다. 원문 날짜의 원형·정밀도·마감 후보·채택 근거는 원본 이력에, 최종 마감 표시값은 공지의 파생 데이터에 둔다.

### 6.3 연락처

| 테이블 | 역할·주요 필드 | 필수 제약 |
|---|---|---|
| `contact_entries`(업무별 연락처) | 조직, 업무명, 대상 캠퍼스, 사무실·업무시간, 상태 | 번호가 바뀌어도 업무 레코드 식별자는 유지 |
| `contact_channels`(연락 수단) | 전화·이메일·팩스·공식 링크, 원문 표기, 정규화 값, 내선, 우선순위 | 업무별 복수 수단 허용. 번호가 같다는 이유로 기관 합치지 않음 |
| `contact_observations`(연락처 원문 근거) | 출처·원문 저장 키, 추출된 필드와 위치, 관찰·검증 시각, 추출 버전 | 동일 근거의 재수집 중복 방지 |
| `contact_field_evidence`(필드와 근거 연결) | 연락처의 특정 값→원문 근거, 채택 여부 | 표시 필드별 근거 추적 가능 |
| `notice_contact_mentions`(공지의 문의처) | 원본 이력, 문의처 원문·수단, 상시 연락처 연결 선택값 | 상시 연락처 연결이 없어도 공지 문의처 보존 |

연락처 이력은 원문 관찰과 필드 채택 기록으로 보존한다. 전화번호 자체를 연락처의 기본키로 쓰지 않는다. 값 비교만으로 다른 캠퍼스의 동명 행정실을 합치지 않는다.

### 6.4 사용자·운영

| 테이블 | 역할 |
|---|---|
| `profiles`(사용자 소속 설정), `subscriptions`(관심 출처 구독), `bookmarks`(저장한 공지) | 인증 서비스의 사용자 식별자와 연결, 사용자별 유일 제약 |
| `runs`(실행 기록), `source_runs`(출처별 수집 이력) | 실행 시작·종료·결과·코드 커밋, 출처별 시도 시각·성공 여부·실패 사유·건수 |
| `outbox_events`(저장 후 후속 처리 기록) | 캐시 무효화·검색 재생성 등을 데이터 변경과 같은 트랜잭션으로 예약 |
| `admin_memberships`(운영 권한), `audit_logs`(운영 감사 이력) | 권한·변경 전후·사유·요청 식별자 |
| `correction_reports`(오류 신고) | 공지·연락처·출처 대상, 내용, 처리 상태. 불필요한 신고자 개인정보 수집 금지 |

인덱스는 원본 유일키, 작업 상태+예약 시각, 공지 노출 시각+식별자, 출처·주제·대상 연결, 연락처 조직·업무 검색을 우선한다. 검색문은 별도 파생 열에 저장한다. 확장 검색은 실제 한글 자료와 실행 계획으로 판단한다.

## 7. 수집·예약·복구 설계

### 7.1 실행 단위의 경계

- 사용자 조회는 미리 생성한 정적 JSON과 데이터베이스 읽기만 사용한다. 사용자 요청이 학교 사이트 수집을 유발하지 않는다.
- 수집 실행은 GitHub Actions 예약 워크플로 1개다. 매 실행은 (1) 실행 시각이 된 출처 선택 → (2) 목록·상세·연락처 수집 → (3) 정규화·분류·중복 → (4) 정적 JSON 생성·업로드 → (5) 실행 결과 기록 순서로 한 프로세스에서 돈다.
- 실행 시간 상한은 워크플로 `timeout-minutes` 40분으로 두고, 내부적으로 30분이 지나면 새 출처 처리를 멈추고 4·5단계로 넘어간다. 남은 출처는 다음 실행에서 우선한다.
- 인스타그램·이미지 처리는 별도 워크플로로 분리해 웹 수집 실행 시간을 잠식하지 않는다.
- 외부 자료 처리는 실행 서버의 임시 폴더에서만 하고 저장소 작업 디렉터리에 쓰지 않는다. 실행 서버는 매번 새로 만들어지므로 상태는 전부 데이터베이스와 R2에 둔다.

### 7.2 평상시 수집과 누락 복구

1. 첫 등록은 최근 90일을 기본으로 하되 진행 중 고정 공지는 기간 밖에서도 수집한다. 기간·페이지 상한에 걸리면 수집 범위 미완료를 남긴다.
2. 정상 순회는 첫 페이지에서 시작하되 이미 아는 일반 게시물 구간과 겹치는 페이지까지 탐색한다. 고정 공지만 만났다고 탐색을 종료하지 않는다.
3. 게시물 번호가 증가한다고 가정하지 않는다. 제목·목록 지문·날짜·알려진 식별자를 함께 이용하고 출처별 종료 규칙을 검증한다.
4. 장애 후에는 마지막 완전 탐색 구간까지 여러 페이지를 읽는다. 페이지 한도를 넘으면 다음 복구 작업으로 이어가고 누락 가능 상태를 유지한다.
5. 최근 14일·고정 공지·진행 중 공지는 하루 1회 상세를 다시 확인한다. 목록에서 수정 신호가 보이면 더 일찍 재확인한다.
6. 오래된 원문은 회전 점검한다. 1차 보장 범위는 신규 및 최근 수정 감지이며, 모든 과거 수정의 즉시 감지는 보장하지 않는다.
7. 전체 상세 성공과 목록 스캔 완료를 구분한다. 상세 일부 실패는 재시도 대상으로 남고 수집 완료 시각을 거짓 갱신하지 않는다.
8. 동일 파일 주소의 교체도 재검증 대상으로 본다. 파일 전부를 15분마다 다시 받지는 않는다.

시간 기반 경계만으로 탐색을 끝내기 어려운 게시판은 날짜 없는 글·재정렬·뒤늦게 등록된 과거 날짜 게시물을 포함한 회귀 자료를 둔다. 초기 이력 90일은 제품에 표시해 전체 역사 수집으로 오해하지 않게 한다.

### 7.3 주기·요청 예산의 시작값

| 대상 | 기본 주기 | 보완 |
|---|---|---|
| 본부·학사·장학 등 주요 공지 | 1시간(매 실행) | 예약 지연 최대 30~60분을 더해 '2시간 내 반영'을 제품 표기 기준으로 삼음 |
| 일반 학과·기관 공지 | 1시간(매 실행) | 요청량이 문제 되면 2시간으로 조정 |
| 저빈도 연구기관 | 6시간 | 신규 발생 패턴에 따라 조정 |
| 연락처 안내 | 7일 | 개편·오류 신고 시 즉시 재확인, 14일 미확인은 오래됨 표시 |
| 복구·초기 대량 수집 | 낮은 우선순위 | 정기 신규 수집과 조회 자원 우선 확보 |

모든 출처를 1시간마다 확인하면 200 × 24 = 하루 4,800회, 30일 약 144,000회 목록 요청이다. 목록이 평균 100KB이면 약 14GB/월이며 원문 서버 부담은 15분 안 대비 1/4이다. 실행 시간은 검증에서 목록 1건 3~4초(미국 실행 서버→서울)였으므로 동시 요청 5개·간격 2초에서 200개 출처 목록은 약 4~5분, 신규 상세 포함 10분 안이 예상값이다. 실측 후 조정한다. 연락처 페이지는 주 1회 별도 실행이다.

동일 원문 서버 묶음은 기본 동시 요청 1개, 요청 사이 2초 이상, 전체 웹 동시 요청 5개로 시작한다. 실행 서버의 공인 주소는 매번 바뀌는 미국 주소이므로 출처 등록 때 `User-Agent`에 서비스 이름·연락 주소를 넣어 운영 주체를 밝힌다. 하위 도메인이 같은 기반 서버를 쓰면 묶어서 제한한다. 서버가 제공하는 재시도 대기값을 우선하고, 조건부 요청을 지원하면 변경 없음 응답을 활용한다.

### 7.4 실행 단위와 상태 기록

작업 대기열 서버·작업 테이블을 두지 않는다. 대신 다음 세 가지로 같은 보장을 얻는다.

- **실행 겹침 방지**: 워크플로에 `concurrency: { group: collect, cancel-in-progress: false }`를 두어 이전 실행이 끝나기 전에는 새 실행이 대기한다. 데이터베이스에도 `runs` 테이블에 실행 시작·종료·결과를 남기고, 시작 시 30분 이상 종료되지 않은 이전 실행이 있으면 '비정상 종료'로 표시한 뒤 진행한다.
- **출처별 상태**: `sources`에 마지막 성공 시각, 마지막 시도 시각, 연속 실패 횟수, 다음 시도 가능 시각, 마지막 오류 종류를 둔다. 연속 실패 시 다음 시도 간격을 1→2→4→8시간으로 늘리고 최대 24시간에서 멈춘다. 접근 차단(403·429)은 출처를 `paused`로 바꾸고 운영자 검토 대상으로 남긴다.
- **결과 중복 방지**: 원본 게시물은 출처+원본 식별자 유일키, 이력은 내용 해시로 중복 저장을 막는다. 같은 원문을 두 번 처리해도 유효 레코드는 하나다(4절 13항).

한 실행 안에서 출처 하나의 실패는 다른 출처로 전파되지 않는다. 실행 자체가 실패(실행 서버 장애, 데이터베이스 연결 불가)하면 GitHub이 저장소 소유자에게 실패 알림 메일을 보내고, 다음 정시 실행이 정상 재시도가 된다. 실행 성공 여부와 무관하게 마지막 단계에서 외부 무료 감시 서비스에 완료 신호를 보내며, 신호가 3시간 이상 끊기면 경보한다(18절).

원본 이력 저장·현재 이력 교체·중복 묶기는 한 트랜잭션에 넣는다. 객체 저장 성공 후 데이터베이스 저장 실패로 남는 파일은 다음 실행의 정리 단계에서 참조 검사로 지운다. 데이터베이스만 성공하고 증거 파일이 없는 상태는 공개하지 않는다.

출처 수집을 중지하면 상태를 `stopped`로 바꾸고 다음 실행부터 건너뛴다. 수집 중지는 기존 공개 자료의 숨김과 구분한다. 출처 비공개·폐쇄 시에는 원본·공지 묶음·연락처 근거·개인 구독·검색·정적 파일 영향 목록을 계산한 별도 운영 명령을 사용한다.

## 8. 원문 추출·정규화·분류·검색

### 8.1 공통 추출 계약

어댑터는 원본 식별자·주소·제목·날짜 원문·본문·첨부·고정 여부·추출 근거·다음 페이지 정보를 반환한다. 저장과 중복 처리 권한은 공통 계층이 가진다. 연락처 어댑터는 업무 단위 필드와 원문 내 위치를 반환한다.

한국어 문자 인코딩, 상대 주소, 게시판 식별용 질의 매개변수, 표·줄바꿈·목록·첨부를 보존한다. 주소에서 추적용 항목만 제거하고 게시물 번호·게시판 번호를 지우지 않는다. 본문 정규화는 공백과 공통 장식을 줄이되 학년도·회차·캠퍼스·마감·대상·부정 표현은 보존한다.

원문 HTML(웹 문서)은 비공개로 보관한다. 사용자에게는 안전하게 정리한 본문과 일반 텍스트를 반환한다. 스크립트·폼·임의 삽입 프레임·이벤트 속성을 제거하고 링크 형식을 제한한다. 표와 이미지가 중요한 공지는 원문과 응답의 실제 표시를 대조한다.

### 8.2 분류·대상·정렬

주제 우선순위는 원 게시판 카테고리 → 제목 말머리 → 명시적인 제목·본문 키워드 → 기타다. 운영자 보정은 최종 표시층에서 우선한다. 규칙명·버전·근거를 저장한다. 연락처는 이 주제 분류에 섞지 않는다.

대상 범위는 출처의 명시적 기본 대상에서 시작하고, 게시물의 명시적인 캠퍼스·대상 제한으로 좁히거나 바꾼다. 불확실한 글은 대상 미확정으로 표시하고 전체 공지에서 찾을 수 있게 한다. '행정학과 소속'과 '행정학과만 신청 가능'은 같은 뜻으로 취급하지 않는다.

기본 피드는 서비스에 처음 노출된 시각과 공지 식별자의 내림차순이다. 화면에는 원문 등록일을 별도로 제공한다. 수정·재수집·다른 출처 추가만으로 순서를 올리지 않는다. 원문 등록일순은 별도 정렬 선택으로 지원하되 날짜 미확정은 뒤에 둔다. 출처의 고정 글을 전체 서비스 고정 글로 자동 승격하지 않는다.

### 8.3 검색

- 검색 대상: 공지 제목·본문·기관명, 연락처 업무명·조직명·별칭·번호·이메일.
- 주제·캠퍼스·조직·출처·기간·매체 필터와 검색을 조합한다. 한 필터의 복수 값은 합집합, 다른 종류의 필터는 교집합이다.
- 초기 검색은 한글 유니코드 정규화·공백 처리·약칭 사전·부분 일치·pg_trgm 후보 검색을 조합한다. 해당 확장의 인덱스 지원은 [공식 문서](https://www.postgresql.org/docs/current/pgtrgm.html)를 확인했다.
- 한두 글자 검색은 삼중 문자 인덱스 효율이 낮을 수 있으므로 정확·접두 검색과 제한된 결과를 쓴다. 형태소 수준 의미 검색을 제공한다고 표현하지 않는다.
- 정확 제목·업무명 > 제목·기관명 부분 일치 > 본문 일치 순으로 순위를 정하고 동률 기준을 고정한다.
- 초기에는 공지와 연락처를 별도 조회한다. 통합 검색이 필요하면 두 종류를 구분한 응답 영역으로 추가한다.
- 검색어 최대 100자·페이지 최대 100건·질의 시간 제한을 두고 실제 한글 질의 자료로 품질과 성능을 검사한다.

## 9. 중복 공지 처리와 되돌리기

### 9.1 1차 자동 병합

같은 출처의 같은 게시물은 원본 식별 단계에서 처리한다. 서로 다른 원본을 하나의 공지로 묶을 때는 충분한 본문 길이, 내용 일치, 대상·학년도·회차·마감의 비충돌을 함께 확인한다. 본문 해시가 같아도 공통 서식만 있는 글은 제외한다.

첨부 파일 해시는 보조 신호다. 동일 신청서가 다른 모집에 쓰일 수 있다. 이미지 지각 해시도 같은 포스터 틀에 날짜만 바뀐 사례가 있으므로 단독 자동 병합 근거로 쓰지 않는다.

유사 제목·본문 후보는 1차에서 운영 검토로 보낸다. 기존 문서의 96%·91% 예시는 확정 임계값이 아니다. 실제 중복/비중복 검증 쌍으로 임계값을 정하고, 잘못 합치는 비용을 우선 낮춘다.

### 9.2 묶음의 대표와 대상

대표 출처는 원문이 살아 있고 본문이 충분한 공식 출처를 우선하되, 순위·최초 게시일·안정 식별자로 결정한다. 운영자가 근거를 남겨 바꿀 수 있다. 출처 목록의 순서도 같은 규칙으로 고정한다.

출처를 합치면서 서로 충돌하는 캠퍼스·마감 정보를 무조건 합집합으로 만들지 않는다. 충돌은 자동 병합 중단 사유다. 학과의 추가 안내가 원문의 대상·내용을 바꾸면 별도 공지 또는 검토 대상으로 남긴다.

### 9.3 병합·분리·삭제 영향

- 병합: 기존 공지 주소를 새 공지로 연결하고, 저장한 공지는 유실 없이 연결한다. 어떤 원본을 보고 저장했는지 근거를 남긴다.
- 분리: 각 원본을 새 묶음으로 이동한다. 원본 근거가 있는 저장 항목은 해당 묶음을 따라가고, 묶음 단위만 저장한 항목은 기존 대표의 묶음을 유지하며 관련 분리 공지를 알려준다. 조용히 삭제하지 않는다.
- 원문 수정: 내용이 달라지면 중복 관계를 재평가한다. 자동 연쇄 병합으로 A와 B, B와 C가 비슷하다는 이유만으로 A·B·C를 모두 합치지 않는다.
- 원문 삭제: 반복적인 삭제 응답·명시적 철회·운영 확인을 구분한다. 하나의 출처 삭제로 다른 출처의 살아 있는 원본을 지우지 않는다.
- 처리 순서: 관계 갱신 → 대표·주제·대상·검색문 재계산 → 이전 주소 연결 → 저장 항목 처리 → 캐시 무효화 → 실제 응답 검증.

## 10. 연락처 분류의 상세 처리

연락처는 기관의 '어떤 업무를 어디로 문의하는가'를 나타낸다. 조직 전체 대표, 장학 담당, 학적 담당, 사무실 안내를 별도 업무로 관리한다.

경희대 공식 [교내 전화번호 안내](https://www.khu.ac.kr/kor/user/contents/view.do?menuNo=200092)에서 캠퍼스별 전화 체계와 내선·범위 표기 형태를 확인했다. 이 자료는 모델 설계 근거이며 이 계획에서 전체 전화번호를 가져오거나 실제 통화를 검증하지 않았다.

| 상황 | 처리 |
|---|---|
| 대표 전화·내선만 제공 | 원문 표기와 내선 분리. 캠퍼스별 외부 발신 규칙이 확인된 경우만 발신 번호 생성 |
| 축약 범위·복수 번호 | '3003~4' 같은 원문을 보존. 규칙이 명확하고 검산된 경우만 복수 번호로 확장 |
| 전화와 팩스 혼재 | 수단 유형을 분리하고 팩스에 전화 걸기 링크를 만들지 않음 |
| 같은 업무의 여러 근거 | 공식 업무 페이지·전화 안내의 우선순위를 출처별로 지정. 값 충돌 시 한쪽을 몰래 삭제하지 않음 |
| 여러 부서가 같은 대표 번호 사용 | 연락 수단을 공유할 수 있으나 부서는 분리 |
| 개인 이름·개인 휴대전화 | 업무 공개 맥락과 필요한 표시 범위를 검토. 기관 대표 연락처로 자동 등록하지 않음 |
| 공지에만 있는 문의처 | 해당 원본 이력의 문의처로 저장. 상시 목록 승격은 공식 근거 또는 운영 검토 필요 |
| 원문이 잠시 실패 | 기존 값 유지, 확인 지연 표시. 마지막 시도 시각과 실제 확인 시각 분리 |
| 정상 페이지에서 항목이 사라짐 | 같은 위치의 정상 파싱이 2회 연속 확인되면 폐기 후보로 검토. 분석 실패는 횟수에 포함하지 않음 |
| 전화·위치 변경 | 업무 식별자 유지, 필드 근거와 변경 이력 갱신, 연락처 캐시 무효화 |

처리 상태는 검증됨·확인 오래됨·충돌 검토 중·비공개·폐기됨이다. 검증됨은 공식 웹 원문과 일치함을 뜻하며 전화 연결·메일 수신이 보장됨을 뜻하지 않는다. 휴무일·업무시간은 원문 정보가 없으면 비워 둔다. 이메일 자동 수집 제한이 있는 출처는 해당 항목의 수집 범위를 별도로 기록한다.

검색은 기관·캠퍼스·업무·전화·이메일을 지원하고, 전화 검색은 기호를 제외한 비교를 추가한다. 정규화된 발신 번호가 확실할 때만 `tel:`(전화 연결 주소), 유효한 이메일에만 `mailto:`(메일 작성 주소)를 제공한다.

## 11. 인스타그램·이미지·마감 정보

### 11.1 인스타그램

**MVP 제외.** 아래 내용은 3차 이후 착수 시점의 설계 기준이며, 1차 웹 공개 일정에는 인스타그램 조사·연동 작업을 넣지 않는다. 재개 조건은 다음 중 하나가 충족될 때다. (1) 대상 학생회 계정이 프로페셔널 계정으로 전환되어 business_discovery 조회가 가능함이 실제 계정에서 확인됨. (2) 게시물 URL 기반 oEmbed 조회가 토큰·심사 없이 가능함이 Meta 공식 문서로 확인되고, URL을 공급할 등록 경로(운영자·기관 제출)가 마련됨. 어느 경우든 계정 단위 스크래핑은 재개 조건에 포함하지 않는다.

웹 출처와 같은 저장 계약을 사용하되 접근 방식·토큰·수집 상태·속도 제한을 독립시킨다. 공식 계정인지, 가능한 연동 방식인지, 계정 소유자 연결이 필요한지, 조회 가능한 게시물 범위가 무엇인지 출처별로 기록한다.

첫 조사 단계에서 공식 연동 1개를 끝까지 시험한다. 계정·권한·심사 조건이 충족된 경우에만 연동을 구현하며, 가능 여부 미확정이면 '연결 대기'로 남긴다. 웹 조회·연락처 출시를 기다리게 하지 않는다. 기관이 제공한 게시물 주소·본문·이미지는 출처와 제공 근거를 붙인 수동 등록 경로로 수용할 수 있다.

Meta(인스타그램 운영사)의 [인스타그램 로그인 연동 문서](https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login/)와 [다른 비즈니스 계정 조회 문서](https://developers.facebook.com/docs/instagram-platform/instagram-api-with-facebook-login/business-discovery/)는 이번 확인에서 요청 제한 응답을 반환했다. 따라서 현재의 세부 권한·심사 조건은 확인 완료로 처리하지 않는다. 비공개 게시물 접근·차단 우회·임의 공개 계정 전체 수집을 기반 가정으로 삼지 않는다.

연동 활성화 전에는 게시물 주소·본문·게시 시각·여러 이미지·수정·삭제·연결 해제·토큰 만료를 실제 허용된 계정에서 검증한다. 외부 이미지 주소는 만료될 수 있으므로 재확인 시각과 표시 불가 상태를 제공한다.

### 11.2 이미지·첨부

초기에는 첨부 링크·파일명·종류·크기를 저장하고 원문으로 이동하게 한다. 원문 재처리와 검증에 필요한 파일만 크기·형식·수집 허용 범위 안에서 비공개 보관한다. 사용자가 임의 주소를 넣어 서버에 다운로드시키는 공개 기능은 만들지 않는다.

첨부 해시와 이미지 비교는 별도 작업에서 처리한다. 다운로드 크기·압축 해제 크기·처리 시간·재시도 횟수를 제한한다. HWP(한글 문서) 본문 변환은 1차에 강제하지 않고 지원 여부를 응답으로 구분한다. 추후 OCR(이미지 문자 인식)은 원문·추출문·도구 버전을 보존하고 낮은 품질의 추출문을 확정 마감·연락처로 자동 채택하지 않는다.

### 11.3 날짜·마감

원문 날짜 문자열, 해석한 날짜, 시간대, 정밀도를 함께 저장한다. 등록일이 날짜뿐이면 날짜와 정밀도로 반환하고 원문에 없는 시각을 표시하지 않는다. 시간대는 저장 시 UTC(세계 표준시), 사용자 표시·규칙 판정은 Asia/Seoul(한국 시간)을 기본으로 한다.

마감은 신청·접수·제출 등 명시적인 기간에 연결된 경우만 추출한다. 시작·종료·행사일·발표일을 구분하고, 연도 없는 날짜·자정 표현·여러 신청기간·상시모집은 별도 검토한다. '날짜만 있음'과 '정확한 시각 있음'을 구분하고 종료 시각을 임의로 23:59로 만들지 않는다. 재추출 시 근거와 규칙 버전을 남긴다.

## 12. 프론트와의 연동 계약

프론트는 화면을 독립적으로 만들고, 백엔드는 고정된 응답 계약·가상 응답 자료·검증용 정적 파일 주소를 제공한다. 데이터베이스 테이블을 그대로 화면 계약으로 노출하지 않는다.

제공 방식은 두 갈래다. **공개 읽기**는 매 실행이 생성한 정적 JSON 파일을 Worker 주소(`https://<이름>.workers.dev/v1/...`, 도메인 확보 시 교체)에서 받는다. 아래 표의 `GET` 경로는 파일 경로에 그대로 대응한다(`GET /v1/notices/{id}` → `/v1/notices/{id}.json`). **검색·신고·개인 기능**은 Supabase의 자동 REST를 읽기 전용 익명 키와 행 접근 정책으로 사용한다. 두 갈래 모두 같은 Pydantic 모델에서 생성·검증되므로 프론트가 보는 형식은 하나다. 사람이 읽는 초안은 [별도 문서](KHU_API_CONTRACT_DRAFT.md)에 두며, 구현 0단계에서 OpenAPI(연동 명세)와 JSON Schema(자료 형식 정의)로 기계 검증 가능하게 만든다.

| 기능 | 경로 초안 | 인증 |
|---|---|---|
| 캠퍼스·분류·기능 활성 여부 | `GET /v1/catalog`(분류 사전 조회) | 불필요 |
| 조직 탐색 | `GET /v1/organizations`(조직 목록), `GET /v1/organizations/{id}`(조직 상세) | 불필요 |
| 출처 목록·상태 | `GET /v1/sources`(출처 목록), `GET /v1/sources/{id}`(출처 상세) | 불필요 |
| 공지 조회 | `GET /v1/notices`(최근 공지 목록, 정적), `GET /v1/notices/{id}`(공지 상세, 정적) | 불필요 |
| 공지 검색 | 정적 `GET /v1/notices/index`(최근 1년 제목·조직·분류·날짜만 담은 압축 색인, 약 1MB)를 프론트가 내려받아 기기에서 검색. 본문 검색은 Supabase REST의 `pg_trgm` 뷰 | 불필요 |
| 비회원 맞춤 조회 | 서버 요청 없음. 프론트가 `/v1/notices/index`와 `/v1/catalog`의 조직 트리로 기기에서 필터링. 기존 `POST /v1/feeds/preview`는 정의만 유지하고 1차에서 구현하지 않음 | 불필요, 서버 저장 없음 |
| 연락처 검색·조회 | `GET /v1/contacts`(연락처 목록), `GET /v1/contacts/{id}`(연락처 상세) | 불필요 |
| 오류 신고 | Supabase REST로 `reports` 테이블에 삽입만 허용(행 접근 정책). 남용 제한은 Supabase 요청량 제한 + 하루 건수 상한 트리거 | 선택 |
| 계정의 내 공지·설정 | `GET /v1/me/feed`(내 공지), `GET /v1/me/profile`(설정 조회), `PATCH /v1/me/profile`(설정 변경) | 필요, 2차 |
| 계정의 관심 출처 | `GET /v1/me/subscriptions`(구독 조회), `PUT /v1/me/subscriptions/{source_id}`(추가), `DELETE /v1/me/subscriptions/{source_id}`(해제) | 필요, 2차 |
| 저장한 공지 | `GET /v1/me/bookmarks`(저장 목록), `PUT /v1/me/bookmarks/{notice_id}`(저장), `DELETE /v1/me/bookmarks/{notice_id}`(해제) | 필요, 2차 |
| 비회원 설정 가져오기·탈퇴 | `POST /v1/me/import`(기기 설정 가져오기), `DELETE /v1/me`(계정 삭제 요청) | 필요, 2차 |
| 운영 기능 | 1차에서는 연동 경로 없음. 저장소의 명령 도구와 `workflow_dispatch`(수동 실행) 입력으로 처리(14절) | 저장소 쓰기 권한 |

공통 규칙:

- 모든 식별자는 문자열, 시간은 표준 시각 문자열, 알 수 없는 값은 `null`(정보 없음), 빈 목록은 빈 배열이다. 임의의 빈 문자열·0·오늘 날짜로 대체하지 않는다.
- 정적 목록은 페이지 파일로 나눈다: `/v1/notices/page/1.json`(최신 50건)부터 순서대로, 각 파일에 `next` 파일명과 `revision`(생성 실행 번호)을 넣는다. 매 실행이 전체 페이지를 다시 생성하므로 실행 도중 두 개정이 섞이지 않게 새 개정을 `/v1/r/<revision>/...` 아래에 먼저 올린 뒤 `/v1/latest.json`의 포인터만 바꾼다. 프론트는 첫 요청에서 `latest.json`을 읽고 그 개정 경로만 따라간다.
- Worker는 `latest.json`에 `max-age=60`, 개정 경로 파일에 `max-age=31536000, immutable`(개정별 불변)을 붙인다. 오래된 개정은 24시간 뒤 정리한다.
- 서명된 `cursor`, 409 개정 충돌 응답은 Supabase REST 경로(검색)에만 남기고 정적 경로에서는 사용하지 않는다.
- 오류는 오류 코드·한국어 안내·요청 식별자·재시도 가능 여부를 공통 형식으로 반환한다. 존재하지 않음, 원문만 사라짐, 서비스에서 비공개됨을 구분한다.
- 공개 출처 상태는 마지막 성공·갱신 지연·연결 가능 여부만 제공한다. 내부 오류 본문·토큰·원문 인증 정보는 공개하지 않는다.
- 계약 변경은 버전과 변경 기록으로 관리한다. 선택 필드 추가를 우선하고 기존 필드 삭제·의미 변경은 새 주 버전에서 한다.

프론트 전달물: 연동 명세, 한국어 필드 사전, 정상·빈 목록·정보 없음·중복 묶음·연락처 충돌·수집 지연·이어보기·비공개·인증 만료 예시, 고정된 자료 식별자, 검증용 Worker 주소와 Supabase 프로젝트 주소·익명 키. 실제 화면 연결 전까지 '사용자 화면 검증 완료'라고 표시하지 않는다.

## 13. 개인화·인증·권한

### 13.1 비회원부터 제공

비회원은 캠퍼스·단과대·학과·추가 구독을 프론트의 기기 저장소에 보관한다. 서버에는 맞춤 목록을 요청할 때만 전달한다. 공개 자료 필터링이므로 계정 생성 없이 이용 가능하다. 서버에 영구 비회원 프로필을 자동 생성하지 않는다.

내 공지 후보는 대학 전체 대상 + 선택 캠퍼스 전체 대상 + 소속 조직과 조상 조직의 대상 + 명시적으로 구독한 출처다. 최종 결과에서는 공지 묶음 식별자로 중복을 제거한다. 전체 캠퍼스·전체 학과의 모든 자손 글을 자동으로 섞지 않는다. 명시적 대상 제한과 미확정 상태는 응답에 남기며 이 기능을 신청 자격 판정으로 표현하지 않는다.

### 13.2 선택적 계정 동기화

2차 기본안은 같은 Supabase 프로젝트의 Auth(계정 인증 서비스, 월 활성 사용자 5만 명까지 무료)에서 소셜 로그인 1종부터 연결하는 것이다. 개인 설정·구독·저장 항목은 사용자 식별자 기준 행 접근 정책을 건 테이블에 두고 프론트가 Supabase REST로 직접 읽고 쓴다. 별도 응용 서버는 두지 않는다. 사업자 계정·제공자 설정이 확정되기 전에는 로그인을 1차 공개의 필수 조건으로 만들지 않는다. 비밀번호 인증 서버를 직접 만들지 않는다.

토큰 검증은 Supabase가 REST 앞에서 수행하고, 데이터 소유권은 행 접근 정책의 `auth.uid()` 비교로 강제한다. 정책이 없는 테이블은 익명·인증 사용자 모두에게 노출되지 않도록 기본 권한을 회수하고, 정책 누락을 자동 검사로 막는다. 사용자가 보낸 사용자 번호나 역할 문자열을 신뢰하지 않는다. 인증 방식의 기준은 [공식 토큰 문서](https://supabase.com/docs/guides/auth/jwts)로 삼고 구현 때 사용 라이브러리의 검증 동작을 시험한다.

운영자는 일반 사용자와 분리한 권한 테이블과 다중 인증을 사용한다. 조회·출처 관리·콘텐츠 보정·시스템 관리 권한을 구분한다. 1차 운영 기능에는 운영자 인증이 필요하며, 일반 사용자 로그인을 나중에 도입한다는 이유로 운영 경로를 공개하지 않는다.

데이터베이스 역할은 수집 실행(쓰기)·백업(읽기)·익명 조회(정책 뷰만)·인증 사용자(본인 행만)로 구분한다. 수집 실행용 비밀 연결 문자열은 GitHub Actions 비밀값에만 두고 프론트에는 익명 키만 배포한다. 내부 테이블(원문 이력, 실행 기록, 보정 기록)은 REST에 노출하지 않는 스키마에 두고, 공개용 뷰만 노출 스키마에 만든다.

비회원 설정 가져오기는 로그인 후 명시적 동작으로 한 번만 수행하고 재시도 중복을 막는다. 구독은 합치되 기존 계정 소속과 충돌하는 기기 소속은 자동 덮어쓰지 않는다. 설정 변경에는 버전 검사를 두어 다른 기기의 오래된 저장이 최신 설정을 지우지 못하게 한다.

탈퇴 요청은 먼저 응용 계정을 비활성화해 기존 토큰으로도 접근을 거절하고, 개인 설정·구독·저장 항목 및 인증 계정 삭제를 후속 작업으로 완료한다. 부분 실패는 재시도한다. 백업에 남는 데이터의 만료 기간은 보존 정책에 명시하고 복구 시 삭제 요청을 다시 적용한다.

## 14. 운영자 기능과 보안 경계

1차 MVP에서는 운영자 화면과 관리 연동 기능을 만들지 않는다. 운영 동작은 저장소의 명령 도구(`python -m khu.ops ...`)를 (가) 운영자 PC에서 수집 실행용 연결 문자열로 실행하거나, (나) `workflow_dispatch` 입력으로 Actions에서 실행한다. 모든 운영 명령은 `corrections`·`audit_log`에 사유·실행자·실행 번호를 남기고, 다음 수집 실행에서 정적 파일에 반영된다. Supabase 대시보드의 표 편집기로 행을 직접 고치는 것은 긴급 시에만 허용하고 감사 기록을 수동으로 추가한다. 운영자 화면은 2차 이후 프론트 범위다.

| 운영 기능 | 필수 동작 |
|---|---|
| 출처 등록·수정 | 설정 미리보기, 원문 표본 추출, 차이 확인, 버전 검사, 적용·되돌리기 |
| 수집 상태 | 마지막 성공, 실패 종류, 미수집 상세, 누락 복구 진행, 최근 이력 조회 |
| 재실행·중지 | 출처·기간·작업 종류 범위 지정, 예상 요청량, 중복 방지, 중지 상태 저장 |
| 중복 검토 | 원문 나란히 보기용 응답, 병합·분리 영향 목록, 사유와 감사 기록 |
| 연락처 검토 | 충돌 값·원문 근거·채택 이력, 항목 수정·비공개·폐기 |
| 오류 신고 처리 | 접수·검토·해결·반려 상태, 관련 원문과 보정 연결 |
| 재처리 | 규칙 버전별 미리 계산, 전후 통계·차이 확인, 출처 일부부터 적용 |
| 감사 | 누가·언제·무엇을·왜 바꿨는지, 원본 이력·배포 버전 연결 |

수집 보안은 다음을 실제 검사한다.

- SSRF(서버를 통한 내부 주소 접근) 방지: 등록된 출처의 허용 도메인·포트만 요청하고 사설·로컬·클라우드 메타데이터 주소를 거절한다. DNS(도메인 주소 조회) 결과·리디렉션 매 단계·첨부 주소에도 같은 검사와 연결 시점 주소 검증을 적용한다.
- XSS(웹 문서의 악성 스크립트 실행) 방지: 원문 HTML은 격리하고 공개 본문을 정리한다. 출처 제목·파일명·검색 강조에도 동일한 출력 원칙을 적용한다.
- 요청·응답·다운로드 크기, 압축 해제 크기, 접속·읽기 시간, 본문 분석 시간을 제한한다. 기본값은 표본 조사 후 출처 설정에 고정한다.
- CORS(브라우저 교차 출처 허용): 정적 공개 파일은 Worker에서 모든 출처 허용(공개 자료). Supabase REST는 승인된 로컬·검증·운영 프론트 주소만 등록한다.
- 비밀값(데이터베이스 연결 문자열, R2 키, Supabase 서비스 키)은 GitHub Actions 비밀값과 Cloudflare 비밀값에만 두고 저장소·공개 응답·실행 로그·가상 자료에 넣지 않는다. 공개 저장소이므로 실행 로그에 비밀값이 찍히지 않도록 출력 마스킹을 확인한다. 개발·검증·운영의 키를 분리하고 유출 시 교체 절차를 둔다.
- 검색은 매개변수 질의, 허용된 정렬만 사용한다. 공개 조회·맞춤 조회·신고·로그인·운영 경로별로 요청량 제한을 나눈다.
- 초기 제한안: 정적 파일은 제한 없음(Worker 무료 구간 하루 10만 요청이 사실상 상한). 검색 뷰는 Supabase 요청량 제한 기본값, 신고는 데이터베이스 트리거로 주소 해시당 시간당 5회. 학교 공용망 사용자를 과도하게 막지 않도록 실제 이용량으로 조정한다.

## 15. 운영 인프라의 구체적 구성

### 15.1 기본 운영 구성

```text
[매시 정각, GitHub Actions 예약 실행 · 미국 실행 서버 · 무료]
  수집 워크플로 (파이썬 1프로세스, 상한 40분)
    1. Supabase에서 출처·상태 읽기
    2. 학교 사이트 목록·상세 수집 (동시 5 · 간격 2초)  ── 검증: 3곳 200 OK, 3~4초/건
    3. 원문 HTML → R2 비공개 버킷 (증거)
    4. 정규화·분류·중복 → Supabase PostgreSQL (무료 500MB)
    5. 공개용 JSON 개정본 생성 → R2 조회 버킷 /v1/r/<revision>/ → latest.json 전환
    6. runs 기록, 외부 감시 서비스에 완료 신호

[사용자 조회 · 서버 없음]
  프론트(Cloudflare Pages 또는 Vercel 무료)
    → Cloudflare Worker (무료, 하루 10만 요청) → R2 조회 버킷의 JSON (캐시·CORS)
    → Supabase REST (검색 뷰, 신고 삽입, 2차 개인 데이터)

[6시간마다, 별도 워크플로]
  pg_dump → 암호화 → R2 백업 버킷 (쓰기 전용 키) · 주 1회 복원 검사

[외부 무료 감시]
  완료 신호 감시(3시간 무신호 경보) · 조회 주소 상태 확인(5분 간격)
```

실행 서버는 GitHub이 매번 새로 제공하는 리눅스(가상 CPU 4개·메모리 16GB급)이며 상태를 남기지 않는다. 지역은 선택할 수 없고 미국 동부·서부에서 실행된다. 2026-09-06 검증에서 미국 실행 서버가 본부·건축공학과·컴퓨터공학부 게시판 목록과 상세를 서울에서와 같은 바이트 수로 받았다(응답 3~4초). 출처를 등록할 때마다 같은 검사를 자동으로 수행해 해외 접근 차단 출처는 `blocked_abroad`로 표시하고 19절의 서버 도입 판단 근거로 모은다.

Supabase 프로젝트는 서울 지역에 만든다. 실행 서버는 IPv4만 제공하므로 데이터베이스 연결은 Supabase의 연결 중계기(세션 방식, 포트 5432)를 사용하고 직접 연결(IPv6 전용)은 쓰지 않는다. [Supabase 연결 안내](https://supabase.com/docs/guides/database/connecting-to-postgres)에 따라 세션 방식에서만 준비된 질의·Alembic 마이그레이션을 실행한다. 연결 수는 실행당 5개 이내다.

무료 프로젝트 한도와 대응:

| 한도 | 값 | 대응 |
|---|---:|---|
| 데이터베이스 크기 | 500MB | 원문 HTML은 R2로, 데이터베이스에는 정규화 본문·메타데이터·검색문만. 350MB에서 경보, 400MB에서 1년 초과 공지의 본문을 R2 참조로 대체 |
| 외부 전송 | 5GB/월 | 사용자 조회는 Worker+R2(무료 전송)로 우회. Supabase 전송은 수집 실행과 검색 뷰만 |
| 미사용 정지 | 1주 | 매시간 수집 쓰기로 발생하지 않음. 수집이 1주 멈추면 정지되므로 18절 경보로 3시간 안에 알아챔 |
| 자동 백업 | 없음 | 17절 자체 백업이 유일한 복구 수단 |
| 프로젝트 수 | 2개 | 운영 1개 + 검증 1개. 검증 프로젝트는 미사용 정지를 허용하고 필요할 때 복구 |

### 15.2 네트워크·도메인·비밀값

- 도메인은 1차 필수가 아니다. Worker는 `*.workers.dev`, 프론트는 `*.pages.dev` 또는 `*.vercel.app` 주소로 시작한다. GitHub Student Developer Pack의 무료 도메인(1년)을 받으면 Cloudflare DNS에 붙여 Worker·프론트에 연결한다.
- 공개 포트·SSH·운영 체제·인증서 관리가 없다. HTTPS는 Cloudflare·Vercel·Supabase가 제공한다.
- R2 버킷은 조회 버킷(Worker 전용 바인딩, 공개 URL 미개방), 증거 버킷(비공개), 백업 버킷(비공개, 쓰기 전용 키)으로 분리한다.
- 비밀값 보관: GitHub Actions 비밀값(데이터베이스 연결 문자열, R2 키, 감시 신호 주소), Cloudflare Worker 비밀값(없음, R2 바인딩만). 프론트 배포에는 Supabase 익명 키와 Worker 주소만 넣는다.
- 저장소가 공개이므로 워크플로 파일·수집 코드·출처 등록 파일은 모두 공개된다. 출처 등록 파일에는 공개된 게시판 주소만 있으며 인증이 필요한 출처 정보는 넣지 않는다. 외부 기여자의 Pull Request에서는 비밀값이 주입되지 않도록 `pull_request` 이벤트에서 수집 워크플로를 실행하지 않는다.

### 15.3 개발·검증·운영 분리

| 환경 | 구성 | 데이터·수집 정책 |
|---|---|---|
| 로컬 개발 | 컨테이너의 PostgreSQL·R2 대체물(MinIO 등)·같은 파이썬 코드 | 고정 원문 자료·가상 사용자, 외부 수집 기본 꺼짐 |
| 자동 검사 | Actions의 Pull Request 검사에서 매번 새 데이터베이스 컨테이너와 고정 자료 | 네트워크 없이 재현, 운영 비밀값 없음 |
| 검증 | Supabase 무료 프로젝트 2번 + R2 검증 버킷 + Worker 미리보기 배포. `workflow_dispatch`로만 실행 | 제한된 실제 출처(10~15개)와 가상 개인 데이터 |
| 운영 | Supabase 운영 프로젝트 + R2 운영 버킷 + Worker 운영 배포. `main` 브랜치의 예약 실행 | 승인된 출처만 |

검증 프로젝트는 정지되어도 무방하며 배포 직전과 월별 복원 훈련 때 깨워 쓴다. 검증에서 운영 데이터베이스를 대신 쓰지 않는다.

### 15.4 캐시와 실패 시 동작

정적 개정 파일은 불변이므로 Worker와 브라우저가 길게 캐시한다. `latest.json`만 60초 캐시이며, 보정·비공개 처리는 다음 수집 실행(최대 1시간 + 예약 지연)에 반영된다. 긴급 비공개가 필요하면 운영 명령이 `workflow_dispatch`로 즉시 실행을 촉발한다. 이 지연은 제품 안내에 명시한다.

수집이 실패하거나 지연되어도 이미 올라간 정적 파일은 그대로 제공되므로 사용자 조회는 멈추지 않는다. `latest.json`에 생성 시각을 넣어 프론트가 '마지막 갱신 N시간 전'을 표시하고, 3시간 이상 오래되면 지연 안내를 띄운다. Supabase가 정지·장애이면 검색·신고·개인 기능만 503(일시 이용 불가)이 되고 공지 열람은 유지된다. '새 글 없음'과 '새 글 확인 불가'는 다른 상태다.

## 16. 비용·용량 계획

2026-09-06 공식 요금 페이지 기준. 모든 항목이 기간 제한 없는 무료 구간 안에 있으며, 무료 체험(기간 한정 크레딧)은 사용하지 않는다.

| 항목 | 월 비용 | 무료 한도와 예상 사용량 |
|---|---:|---|
| 수집 실행 | 0원 | GitHub Actions, 공개 저장소는 실행 시간 무제한. 예상 사용 월 약 720회 × 10분 = 7,200분 |
| 데이터베이스·인증 | 0원 | Supabase 무료: 500MB, 전송 5GB/월, 활성 사용자 5만. 예상 1년 뒤 데이터베이스 200~350MB |
| 원문·백업·조회 파일 저장 | 0원 | R2 무료: 저장 10GB·월, 쓰기 100만·읽기 1,000만 건, 외부 전송 무료. 예상 저장 3~6GB, 쓰기 월 30만 건 |
| 조회 제공 | 0원 | Cloudflare Worker 무료: 하루 10만 요청. 월 1,000명 기준 예상 하루 3~5천 요청 |
| 프론트 호스팅 | 0원 | Cloudflare Pages 또는 Vercel Hobby(비상업 조건) |
| 외부 감시·경보 | 0원 | 무료 완료 신호 감시 1개, 무료 상태 확인 1개, GitHub 실패 알림 메일 |
| 도메인 | 0원(선택) | 없이 시작. Student Pack 무료 도메인 1년, 이후 연 1.5~2만 원 |
| **합계** | **0원** | 학교 지원 전까지 유지 |

초안(2026-09-06 오전)의 월 52~70달러 구성과 1차 수정안(서버 1대, 월 13~15달러)은 폐기한다. 이 구성이 감당하는 상한과 넘었을 때의 첫 조치는 다음과 같다.

| 신호 | 한도 | 첫 조치 (비용) |
|---|---|---|
| 데이터베이스 400MB 도달 | 500MB | 1년 초과 공지 본문을 R2 참조로 대체(0원) → Supabase Pro 8GB(25달러) |
| Worker 하루 8만 요청 | 10만 | 프론트 캐시 연장(0원) → Workers 유료 5달러(1,000만 요청) |
| Supabase 전송 4GB/월 | 5GB | 검색 뷰 응답 축소(0원) → Pro(25달러, 250GB) |
| 수집 실행 30분 초과 | 40분 상한 | 저빈도 출처 6시간 주기로 이동(0원) → 소형 서버 cron으로 이전(월 7달러) |
| 해외 차단 출처 5개 이상 | — | 해당 출처만 서울 서버에서 수집(월 7달러) |
| 1시간+지연이 사용자 불만으로 확인 | — | 서울 서버 cron 15분 주기(월 7~12달러) |

학교 지원이 확정되면 순서는 Supabase Pro(백업·8GB·정지 없음) → 도메인 → 소형 서버(수집 주기 단축)다. 세 단계를 다 해도 월 40달러 안이며 코드 구조는 바뀌지 않는다.

용량 가정:

- 부하 검사 기준: 출처 200개, 원본 공지 10만 건, 연락처 5천 업무, 동시 사용 가정의 조회 초당 20회·5분간 50회. 실제 사용자 수가 확인된 것은 아니다.
- 원본 공지당 데이터베이스 저장량은 정규화 본문·메타데이터·검색문 합계 평균 5KB로 잡는다(원문 HTML 100KB는 R2). 경희대 전체 신규 공지를 하루 100~200건으로 보면 1년 약 5만 건, 약 250MB다. 500MB 안이지만 인덱스·이력이 더해지므로 15.1의 350MB 경보를 둔다.
- 원문 HTML 보관을 평균 100KB×5만 건으로 잡으면 약 5GB다. 압축 저장(gzip, 약 1/5)으로 1GB 안에 두고, 90일 지난 비채택 이력은 삭제한다. 첨부 파일 원본은 1차에서 저장하지 않고 주소·해시만 기록한다.
- 초당 20회는 Worker·Supabase 검색 뷰의 성능 검사용이지 월 사용량 가정이 아니다. 월 사용자 1,000명 × 주 5회 × 요청 5개 = 월 약 10만 요청, 하루 약 3,500회로 Worker 한도의 3.5%다.
- 매월 데이터베이스 크기·R2 저장량·Worker 요청 수·Actions 실행 시간·Supabase 전송량을 기록한다. 각 무료 한도의 70%·90%에서 알리고 위 표의 0원 조치를 먼저 적용한다. 백업을 중단하는 방식으로 절감하지 않는다.
- 무료 구간은 초과 시 서비스가 멈추거나(Supabase, Worker) 청구가 시작되므로(R2) 세 공급자 모두 지출 한도·알림을 켜고, 수집 코드에도 실행당 요청 수·저장량 상한을 둔다.

## 17. 배포·백업·복구

### 17.1 저장소와 배포 흐름

1. Pull Request에서 문법·정적 검사, 원문 회귀 자료, 데이터베이스 제약, 연동 계약 호환성 검사를 새 데이터베이스 컨테이너로 실행한다. 비밀값은 주입하지 않는다.
2. `main` 병합이 곧 배포다. 다음 정시 수집 실행이 새 코드로 돈다. 별도 이미지 빌드·서버 접근이 없다.
3. 스키마 변경은 `main` 병합 뒤 별도 마이그레이션 워크플로가 Alembic을 세션 방식 연결로 실행한다. 수집 실행은 시작 시 스키마 버전을 확인해 코드가 기대하는 버전이 아니면 즉시 종료하고 경보한다.
4. 새 열·테이블 추가 → 구버전과 호환되는 코드 병합 → 기존 자료 채우기 → 사용 전환 → 후속 병합에서 옛 구조 제거 순서를 사용한다. 이미 수집한 데이터를 지우는 역방향 변경으로 되돌리지 않는다.
5. Worker 코드는 Cloudflare의 배포 도구로 Actions에서 배포하며 이전 버전으로 즉시 되돌릴 수 있다. 정적 JSON은 개정 경로가 불변이므로 `latest.json` 포인터를 이전 개정으로 되돌리는 것이 롤백이다.
6. 검증 프로젝트에 먼저 `workflow_dispatch`로 한 실행을 돌려 실제 조회·수집·연락처 갱신·권한 검사를 수행한 뒤 운영 예약 실행에 반영한다. 계약 변경·스키마 변경이 없는 수정은 자동 검사 통과로 충분하다.
7. 실패하면 `main`을 이전 커밋으로 되돌리는 Pull Request를 병합한다. 다음 실행부터 복구된다.

구현 때 만들 문서: 환경 구성, 첫 배포(Supabase·R2·Worker 생성 절차), 스키마 변경, 비밀값 교체, 출처 장애, 대량 재처리, 백업 복원 절차. 외부 Actions 구성요소는 커밋 해시로 고정한다.

### 17.2 보존과 백업

| 데이터 | 초기 보존안 | 복구 방법 |
|---|---|---|
| 현재 공개 공지·연락처·출처 | 활성 기간 전체 | 데이터베이스 백업 + 원문 재처리 |
| 원본 변경 이력 | 기본 1년, 현재 채택 근거는 참조 중 유지 | 이력·규칙 버전으로 재생성 |
| 비공개 원문·처리 증거 | 일반 과거본 90일, 현재 채택 근거·검토 중 자료는 유지 | 객체 목록과 해시 대조 |
| 원문 오류 표본 | 30일, 비밀값 제거 | 재현 자료로 필요한 것만 별도 보존 |
| 작업 상세·서버 로그 | 30일 | 장기 일별 집계는 별도 유지 |
| 운영 감사·보정 기록 | 1년 | 암호화된 별도 백업 |
| 개인 설정·구독·저장 항목 | 계정 유지 기간 | 탈퇴 시 서비스 삭제, 백업 잔존 만료 적용 |

Supabase 무료 요금제에는 자동 백업이 없다. 백업은 전적으로 아래 자체 절차로 이루어지며, 이것이 실패하면 복구 수단이 없다는 점을 경보 조건(18절)에 반영한다.

6시간마다 별도 GitHub Actions 워크플로가 `pg_dump`(논리 백업 도구) 결과를 압축·암호화해 R2의 백업 버킷에 저장한다. 500MB 이하 데이터베이스이므로 실행은 수 분이다. 백업 버킷의 쓰기 전용 자격 증명만 Actions 비밀값에 두고 삭제 권한은 운영자 Cloudflare 계정에만 둔다. 최근 7일은 6시간 단위, 이후 30일까지 하루 1개를 보존한다. 월별 장기 사용자 백업은 기본으로 만들지 않아 탈퇴 후 장기 잔존을 늘리지 않는다. 파일 저장소는 채택 중인 객체를 유지하고 6시간 단위로 목록·해시·별도 사본을 확인한다.

백업 워크플로도 완료 신호를 외부 감시 서비스에 보내 7시간 무신호를 경보한다. 수집 실행의 자격 증명으로 백업을 삭제할 수 없도록 키를 분리한다. 백업 암호화 키의 복호화 키는 Actions 비밀값이 아닌 운영자 개인 보관소에 둔다. 사용자 삭제 요청 기록은 제한된 별도 저장으로 백업 보존 기간 동안 유지해 복원 후 재삭제한다.

### 17.3 복구 목표와 실제 훈련

- RPO(허용 데이터 손실 시간): 6시간 백업이 정상일 때 최대 6시간 목표. 공급자 백업이 없으므로 자체 백업 실패는 곧 복구 불가이며, 7시간 이상 백업 미성공은 즉시 경보한다.
- 백업 완료 시각뿐 아니라 백업이 포착한 데이터 기준 시각을 측정한다. 6시간 간격에 실행 지연·업로드 시간이 더해져 목표를 넘으면 간격을 줄이고, '6시간마다 시작'을 '최대 6시간 손실'의 증거로 사용하지 않는다.
- RTO(복구 완료 시간): 공지 열람은 정적 파일이 남아 있으므로 데이터베이스 장애 중에도 유지된다. 검색·신고·수집 재개는 새 Supabase 프로젝트 생성+복원으로 4시간, 누락 복구는 추가 24시간 안 목표. 실제 측정 전에는 보장값이 아니다.
- 첫 공개 전과 이후 매월 빈 환경에 복원한다. 백업 파일 존재나 업로드 성공만으로 통과시키지 않는다.
- 복원 검증: 스키마·행 수·참조 무결성 → 실제 공지·연락처 조회 → 객체 해시·본문 일치 → 사용자 권한·탈퇴 재적용 → 캐시 초기화 → 작업 임대 정리 → 수집 한 주기 재실행.
- 복구 후 오래된 실행 중 작업은 안전하게 재대기시키고, 복원 시점 이후 공지를 복구 탐색한다. 과거 백업의 오래된 수집 상태를 현재 성공 상태로 표시하지 않는다.
- Supabase Pro로 올라가면 공급자 일별 백업(7일)이 추가되지만 자체 6시간 백업은 유지한다.

계정 기능을 도입할 때는 인증 데이터·소셜 제공자 설정·사용자 식별자 연결의 복원도 별도 시험한다. 응용 테이블 덤프만으로 인증 서비스까지 다른 프로젝트에 복원된다고 가정하지 않는다. 새 인증 환경으로 이전해야 한다면 기존 사용자 식별자 연결을 보존하고 세션을 폐기해 재로그인을 유도하는 절차를 둔다.

## 18. 관측·경보·운영 업무

### 18.1 관측 항목과 목표

| 구분 | 지표·초기 목표 | 경보 시작값 |
|---|---|---|
| 공개 조회 | 월 가용성 99.5% 목표, 점검 중단도 집계 | 외부 조회 3분 연속 실패 |
| 조회 성능 | 실제 기준 자료에서 응답 95백분위: 목록·연락처 500ms, 검색 1초 이내 | 10분 연속 목표 초과 |
| 공지 신선도 | 정상 원문의 신규 글 95%를 2시간 내 반영(1시간 주기 + 예약 지연) | 수집 완료 신호 3시간 무신호 |
| 개별 출처 | 설정 주기의 2배 안에 성공 순회 | 3회 연속 실패, `blocked_abroad` 신규 발생 |
| 내용 추출 | 목록·상세 성공률, 빈 본문 급증, 추출 건수 급감 | 과거 평소량 대비 급락·예상 구조 소실 |
| 수집 실행 | 실행 소요 시간, 시간 상한으로 건너뛴 출처 수, 실행 실패 | 소요 30분 초과, 실행 실패(GitHub 메일) |
| 연락처 | 검증일·충돌·원문 소실·폐기 후보 수 | 14일 미확인, 새 충돌 발생 |
| 데이터베이스 | 크기·전송량·질의 지연 | 350MB·400MB, 전송 4GB/월 |
| 무료 한도 | Worker 일 요청 수, R2 저장·쓰기 수, Actions 분 | 각 한도 70%·90% |
| 백업 | 마지막 성공·파일 검사·마지막 복원 훈련 | 7시간 미성공, 월 복원 누락 |
| 비용 | 세 공급자 청구 예상액 | 0원 초과 예상 시 즉시 |

신선도는 날짜만 있는 글의 게시 시각을 지어내서 계산하지 않는다. 정확한 게시 시각이 없는 출처는 독립 표본 관찰 시각·목록 탐색 간격을 사용하고 측정 방식과 표본 수를 함께 남긴다. 실패 출처를 분모에서 빼서 성공률을 높이지 않는다. 활성 출처 전체 성공률과 원문 서버 장애 영향은 둘 다 보고한다.

공지 열람의 가용성은 Cloudflare에 의존하고 수집 신선도는 GitHub Actions 예약 실행의 최선 노력(best-effort) 실행에 의존한다. 둘 다 무료 구간에는 서비스 수준 보장이 없다. 신선도 목표 미달이 반복되면 19절의 서버 도입 조건으로 넘긴다.

### 18.2 로그와 대응

상태는 정적 파일 `/v1/status.json`(마지막 실행 시각·결과·출처별 마지막 성공·계약 버전·코드 커밋)으로 매 실행이 생성한다. 웹 수집 출처 하나의 장애가 전체 상태를 실패로 만들지 않는다. 내부 오류 본문·비밀값은 넣지 않는다. 상세 실행 로그는 GitHub Actions 실행 화면에서 본다(공개 저장소이므로 로그도 공개된다는 점을 마스킹 검사로 보완).

요청·작업·출처·배포 버전을 같은 식별자로 추적하되 토큰·이메일·전화·사용자 검색 원문·전체 본문을 일반 로그에 남기지 않는다. 원문 근거는 접근 제한된 저장소로 연결한다. 출처 목록의 '정상'은 최근 성공 주기와 복구 미완료 여부로 계산한다.

외부 상태 감시는 GitHub·Cloudflare·Supabase와 무관한 무료 서비스에서 실행한다. 경보는 장애 시작·중요 악화·복구 시 한 번씩 묶고 매 순회 동일 메시지를 보내지 않는다. 수신 채널·운영자 연락처는 배포 단계에서 설정하며 이 계획에서 메시지를 발송하지 않는다.

운영 주기:

- 매일: 신규 실패·누락 복구·연락처 충돌·신고·백업 실패 확인.
- 매주: 출처별 요청량·신선도·원문 표본 대조, 중복 오탐 검토, 로그·용량 점검.
- 매월: 백업 복원 훈련, 취약점·의존성·비용·출처 변경 감사.
- 학기 전환·조직 개편 시: 기관 계층·학과·담당 연락처·학생회 계정·고정 공지 전수 재확인.

## 19. 확장과 장애 유형별 대응

| 신호 | 먼저 할 조치 | 다음 확장 |
|---|---|---|
| 원문 서버의 요청 제한 | 같은 서버 묶음 감속·복구 작업 분산 | 수집 주기 재설계. 서버를 늘려 제한을 우회하지 않음 |
| 수집 실행이 30분을 넘김 | 저빈도 출처를 6시간 주기로, 상세 재확인 축소 | 소형 서버 cron으로 수집 이전(월 7달러) |
| 데이터베이스 400MB·검색 지연 | 오래된 본문 R2 참조화, 인덱스 점검 | Supabase Pro(월 25달러) |
| 해외 접근 차단 출처 발생 | 해당 출처 `blocked_abroad` 표시, 목록에 미수집 명시 | 5개 이상이면 서울 소형 서버에서 해당 출처만 수집 |
| 한글 검색 품질 부족 | 실제 검색어 정답집·약칭·부분 일치 개선 | 독립 검색 색인 검토, 재생성·동기화 계약부터 설계 |
| 이미지·브라우저 처리가 실행 시간 점유 | 별도 워크플로·동시 실행 1개 | 전용 서버 분리 |
| 예약 실행 지연이 신선도 목표 초과 반복 | 실행 시각 분산(정각 회피), 실행 시간 단축 | 서울 소형 서버 cron 15분 주기(월 7~12달러) |
| 원문 파서 공통 오류 | 같은 어댑터 출처 전부 잠시 격리·영향 산정 | 공통 원인 수정 후 표본→전체 재처리 |

출처가 늘 때마다 재구조화하지 않도록 대학·조직 식별자, 출처 설정, 어댑터 등록, 작업 유형을 처음부터 명시한다. 다른 대학 확장은 대학 등록과 새 출처 설정으로 검증하며 공통 계층에 학교 이름 조건을 추가하지 않는다.

## 20. 실행 순서·산출물·의존성

아래 공수는 백엔드 전담 1명 기준의 예상 작업일이다. 자동화 활용으로 줄어들 수 있으나 외부 출처 수·구조·권한 확보에 따라 늘어난다. 일정은 고정 납기 약속이 아니며 각 단계의 통과 조건으로 다음 단계 진입을 결정한다.

| 단계 | 예상 작업일 | 작업·산출물 | 통과 조건 |
|---|---:|---|---|
| 0. 계약·조사 기준 | 3~5일 | 기계 판독형 명세, 응답 자료, 전체 후보 조사표, 대표 10~15개 원문 표본 | 프론트 가상 자료가 계약 검사 통과, 공지·연락처·실패 상태 표현 가능 |
| 1. 기반 구축 | 3~4일 | 파이썬 패키지 골격, Supabase 검증·운영 프로젝트, R2 버킷 3개, Worker 골격, Actions 워크플로(검사·수집·백업·마이그레이션), 스키마·초기 등록·권한 | 빈 데이터베이스 재구성, 잘못된 참조·중복 입력 차단, 수동 실행으로 빈 개정 파일 생성·제공 확인 |
| 2. 실제 수집 한 바퀴 | 5~8일 | 공통 수집 계약, 대표 어댑터, 실행 상태·재시도, 원문 보관·이력, 정적 JSON 생성·개정 전환 | 실제 원문→저장→정적 파일→Worker 조회 완료, 재실행 중복 없음, 실행 실패 후 다음 실행에서 회수 |
| 3. 공지·연락처 기능 | 5~7일 | 전체 공지·검색·기본 중복·연락처·비회원 맞춤 조회·운영 보정 | 원문 표본 1:1 일치, 연락처 변경·중복 분리·대상 필터 검증 |
| 4. 운영 준비 | 2~4일 | 예약 실행 켜기, 무료 한도 알림, 외부 감시, 백업·복구 절차, 60일 비활성 방지 커밋 | 새 Supabase 프로젝트에 복원 후 실제 조회·수집 재개, 비공개·실패 상태 통과 |
| 5. 출처 확대·시범 운영 | 5~10일+관찰 기간 | 조사표 전체 상태 확정, 구조별 설정 확대, 출처별 검사, 프론트 실제 연동 지원 | 공개 대상 전부 검증, 7일 연속 관찰, 누락·오탐·장애 조치 근거 |
| 6. 계정 동기화 | 3~5일 | 로그인·소속 저장·구독·저장한 공지·탈퇴 | 두 사용자·두 기기·토큰 만료·부분 삭제 실패 검사 |
| 7. 인스타그램·고도화 | MVP 제외, 11.1절 재개 조건 충족 후 재산정 | 허용된 실제 계정 연동, 이미지·유사 중복·선택적 마감 추출 | 계정별 실제 수집 증거와 정확도 검증, 웹 장애 격리 |

1차 웹 공개의 기준 공수는 0~5단계 합계 24~39작업일이며, 7일 관찰은 마지막 설정 확대와 일부 겹칠 수 있다. 200개 출처를 몇 개 표본의 평균 시간으로 확정 견적하지 않는다. 공통 형식의 설정 추가와 새로운 형식의 어댑터 개발을 분리해 0단계 조사 결과로 다시 산정한다.

의존 관계: 연동 계약 → 스키마·기반 → 실제 수집·원문 저장 → 공지·연락처 조회 → 운영 복구 → 전체 출처 공개. 프론트 가상 구현은 0단계 규격부터 독립 진행한다. 인프라 견적 확인은 초기에 진행한다. 인스타그램은 MVP에서 제외하며 11.1절 재개 조건이 갖춰진 뒤 별도 일정으로 다룬다.

## 21. 검증 계획과 공개 통과 기준

### 21.1 필수 재현 사례

| 영역 | 검사 사례 | 기대 결과 |
|---|---|---|
| 수집 | 같은 글 3회 수집·실행 2개 동시 촉발·저장 직전 중단 | 원본 1개, 중복 이력·묶음 없음 |
| 누락 | 한 순회 동안 페이지 크기보다 많은 새 글, 긴 장애, 날짜 역전 | 뒤 페이지 복구·미완료 표시·이어하기 |
| 고정·수정 | 오래된 고정 글, 같은 주소의 본문·첨부 교체 | 고정 글에 가로막히지 않음, 변경 이력 생성 |
| 원문 구조 | 진짜 빈 목록, 인코딩 변경, 구조 변경, 응답 200인 차단 페이지 | 빈 목록과 분석·접근 실패 구분 |
| 장애 | 요청 제한·서버 오류·시간 초과·실행 시간 상한·실행 중단 후 재실행 | 감속·격리·다음 실행 재시도, 다른 출처 정상 |
| 중복 | 같은 양식 다른 모집, 학년도만 다른 공지, 같은 틀 다른 날짜 포스터 | 잘못 병합하지 않음 |
| 병합·분리 | 기존 상세 주소·저장한 공지·출처·검색·캐시 존재 | 참조 보존·안내·파생 재계산 |
| 연락처 | 복수 업무, 같은 대표 번호, 내선 범위, 팩스, 번호 변경, 충돌 | 정확한 수단·원문 표기·업무 식별 유지 |
| 연락처 실패 | 일시 요청 실패, 정상 페이지에서 반복 소실, 오래된 확인 | 실패로 삭제하지 않음, 상태 정확 |
| 날짜 | 시각 없는 게시일·마감, 연도 없는 기간, 여러 신청기간 | 임의 정밀도 금지·불확실성 표시 |
| 개인화 | 두 캠퍼스 동명 학과, 공통 기관, 대상 미확정, 구독 해제 | 의도한 범위만 포함, 자격 단정 없음 |
| 계약 | 빈 배열·정보 없음·다중 출처·한국어·이어보기·개정 변경 | 가상 응답과 실제 응답 동일 형식 |
| 보안 | 다른 사용자 식별자, 위조 관리자, 악성 HTML, 내부 주소 리디렉션 | 접근·저장·렌더링 차단 |
| 복구 | 새 서버·빈 데이터베이스·객체 사본에서 복원 | 참조·권한·삭제 요청·수집 재개 검증 |

### 21.2 층별 완료 판정

| 계층 | 수행할 검사 | 남길 증거 |
|---|---|---|
| 정적 | 문법·타입·의존성·계약 변경 | 검사 실행 결과·버전 |
| 콘텐츠·데이터 | 원문과 제목·본문·대상·연락처 1:1 대조, 제약 검사 | 원문 주소·표본·기대값·차이 |
| 실행·직렬화 | 실제 수집→데이터베이스→조회 응답, 반복·경쟁·실패 복구 | 수집 이력·조회 응답·행 수 |
| 운영 저장소·캐시 | 운영 설정·마이그레이션·보정 무효화·복원 | 배포 버전·백업 해시·복원 기록 |
| 실제 사용자 결과 | 별도 프론트의 목록·상세·연락처·원문 이동·설정 저장 | 실제 화면과 응답 대조 기록 |

1차 공개 통과 조건:

1. 공개 범위의 모든 출처에 공식성 근거·설정 버전·정상 표본·마지막 성공이 있다. 보류 출처와 조사 미완료를 별도 집계한다.
2. 같은 형식을 공유하는 새 어댑터는 서로 다른 실제 출처에서 검증한다. 실제 출처가 하나뿐인 독립 형식은 그 사실을 기록하고 여러 실제 페이지·변경 사례로 검증한다. 공통 형식은 후속 출처를 공통 엔진 수정 없이 추가하는 사례를 남긴다.
3. 공개 대상에서 7일 연속 자동 수집을 관찰한다. 독립 표본 대조에서 확인된 신규 누락은 0건이어야 하며, 발견되면 원인 수정·복구 후 영향 출처 관찰을 다시 시작한다. 이는 인터넷 전체 누락 0건 보장이 아니다.
4. 원문 표본 최소 200건을 구조·캠퍼스·기관별로 배분한다. 각 공개 출처는 최소 3건 또는 존재하는 전부를 대조하고, 공지 자체가 없는 출처는 빈 목록 근거를 남긴다.
5. 자동 중복 병합은 검증 쌍 최소 300쌍에서 잘못 합친 사례 0건을 통과해야 한다. 표본 결과를 일반 정확도 보장으로 확대하지 않고 출시 후 오탐을 계속 검토한다.
6. 연락처는 초기 공개 항목 전부를 원문과 대조한다. 불확실한 번호 확장은 비활성화하고, 모든 공개 필드에 근거가 있어야 한다.
7. 기준 부하에서 Worker·검색 뷰 조회 목표를 통과해야 한다. 검증 Supabase 프로젝트(운영과 같은 무료 크기)에서 측정한다.
8. 비공개 처리·사용자 권한·백업 복원·재시작 후 수집 재개가 실제로 통과한다. 문서 존재만으로 완료 처리하지 않는다.
9. 프론트 실제 연동이 아직이면 '백엔드 응답 검증 완료, 사용자 화면 미검증'으로 명시한다. 제품 전체 완료는 화면 연결 뒤 판정한다.

## 22. 구현 산출물의 예상 위치

아래 경로는 앞으로 만들 구조이며 현재 코드가 존재한다는 뜻이 아니다.

| 경로 | 책임 |
|---|---|
| `backend/app/export/`(정적 파일 생성) | 개정 JSON 생성·R2 업로드·`latest.json` 전환 |
| `backend/app/contracts/`(공유 응답 모델) | 계약·검증 모델·직렬화 |
| `backend/app/domain/`(업무 규칙) | 공지·연락처·조직·대상·중복 규칙 |
| `backend/app/ingestion/`(수집 계층) | 웹·인스타그램·연락처 어댑터 |
| `backend/app/run/`(실행 진입점) | 실행 순서·시간 상한·상태 기록·완료 신호 |
| `backend/app/storage/`(저장 계층) | 데이터베이스·객체 저장·보정 적용 |
| `backend/migrations/`(저장 구조 이력) | 스키마·인덱스·권한 변경 |
| `backend/tests/fixtures/`(원문 재현 자료) | 비밀값을 제거한 원문과 기대 추출값 |
| `worker/`(조회 제공) | Cloudflare Worker: R2 바인딩·캐시·CORS, 30줄 내외 |
| `.github/workflows/`(자동 실행) | 검사·수집(매시)·백업(6시간)·마이그레이션·수동 운영 명령 |
| `scripts/fetch_test.py`(접근 검증) | 실행 서버에서 출처 접근 가능 여부 확인. 2026-09-06 3곳 통과 |
| `contracts/`(프론트 전달 규격) | 연동 명세·가상 응답·한국어 필드 설명 |
| `registry/bootstrap/`(최초 출처 등록) | 조사 결과·최초 가져오기 파일 |
| `infra/`(운영 구성) | 컨테이너·요청 전달·환경·백업·서버 초기화 |
| `docs/runbooks/`(운영 절차) | 장애·배포·복원·신규 출처·연락처 보정 |

## 23. 남은 결정과 재검토 시점

| 항목 | 지금 적용한 기본안 | 재검토 시점 |
|---|---|---|
| 연락처의 화면 위치 | 독립 분류·독립 조회 기능 | 프론트 설계에서 배치 결정, 백엔드 모델 영향 없음 |
| 전체 대상 출처와 광릉 범위 | 전체 후보 조사, 공개 범위 명시 | 0단계 조사표 확정 |
| 출처별 본문·파일 재게시 범위 | 원문 보존·안전한 본문·링크 중심, 항목별 이용 조건 기록 | 출처 활성화 전 |
| 정확한 기술 버전 | 안정 지원 버전 고정 | 1단계 골격 구성 |
| 계정·예산 | GitHub·Supabase·Cloudflare 무료 계정, 월 0원. 학교 지원 확정 시 Supabase Pro부터 | 무료 한도 70% 도달 또는 지원 확정 시 |
| 수집 지연 허용 | 1시간 주기 + 예약 지연, 제품에 '2시간 내 반영' 표기 | 시범 운영 7일 관찰 후 실제 지연 분포로 재확인 |
| 소셜 로그인 제공자 | 선택적 로그인 1종 | 2차 계정 동기화 착수 |
| 인스타그램 접근 방식 | MVP 제외 확정(2026-09-06). 재개 시 프로 계정 연동 또는 URL 등록+oEmbed | 11.1절 재개 조건 충족 후 |
| 중복 임계값·과거 수집 범위 | 보수적 병합, 최근 90일 기본 | 원문 검증 자료 확보 후 |
| 운영 경보 수신처 | 채널만 교체 가능한 설정 | 배포 준비 |

이 문서 작성으로 비용 발생 자원을 만들거나, 외부 계정을 연결하거나, 대량 수집·배포·커밋·푸시를 실행한 것은 아니다. 구현을 시작할 때는 0단계 계약과 원문 조사부터 진행한다.
