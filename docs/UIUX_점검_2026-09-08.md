# UI/UX 점검 — 2026-09-08 13:20 KST

실제 사이트(`https://경희공지.info` = workers.dev)를 데스크톱 1280·모바일 375 로 직접 눌러 보고,
`KHU_INTEGRATED_NOTICE_PLAN.md`·`docs/UI_CONCEPT.md`·`docs/세션_인계.md` 5·10~13절·커밋 `51eb1b3` 과
대조했다. 코드는 고치지 않았다. 사용자 요청: "통상적인 정보 사이트에 있는데 우리에게 없는 것,
그리고 지시를 잘못 읽었거나 서로 오해했을 여지가 있는 것을 검사하라."

## 1. 한눈에 — 중요도 상

| # | 항목 | 갈래 | 판단 필요 |
| --- | --- | --- | --- |
| 1 | 조직을 고르면 본부·전교 공지(54건)와 캠퍼스 공지가 내 공지·전체 공지에서 빠진다 | 의도 오해 | 예 |
| 2 | 같은 글이 학과마다 따로 떠서 목록 20건 중 11건이 같은 내용(제목 표기 차이로 병합 실패) | 데이터 | 아니오(제목 정규화 강화) |
| 3 | 병합된 공지의 대상이 대표 게시판 조직 하나뿐이라 다른 조직 필터에서 안 보임(표본 3%) | 백엔드 | 아니오 |
| 4 | 마감일 표시·마감 임박 정렬 없음. 정렬은 "최신순" 글자뿐 | 없는 기능 | 아니오 |
| 5 | 검색어·필터가 주소에 남지 않아 공유·북마크·뒤로 가기 복원 불가 | 없는 기능 | 아니오 |
| 6 | 원문 보기가 상세 맨 아래에만 있음 | 없는 기능 | 아니오 |
| 7 | 0건일 때 이유 없음(캠퍼스 서울 + 국제캠 학과 → 0건) | 없는 기능 | 아니오 |
| 8 | 온보딩 3단계가 첫 진입에 뜸. 9월 6일 결정("마법사 대신 목록에서 체크")과 어긋남 | 의도 오해 | 예 |

## 2. 의도 오해 여지

- **"조직을 고르면 그 조직 공지만"(인계 5절)** 을 9월 8일 두 탭 통일 때 "전교·캠퍼스 대상은 조직을
  고르지 않았을 때만"으로 구현했다(`api.ts` `audiencesInOrganizationScope`). 다른 학과 글이 섞이지
  말라는 뜻이었다면 맞지만, 본부가 내는 학사·장학 공지까지 빼는 것이 의도였는지는 확인이 필요하다.
  제안: 조직을 골라도 `university` 대상은 포함하고, 캠퍼스 대상은 고른 캠퍼스와 맞을 때 포함.
- **"공지 아닌 글 필터링 안 함"(인계 5절)** 과 별개로, 같은 글의 중복이 목록을 채운다. 실측: 공과대학
  목록에 「2026 KHU Homecoming Week」 6줄, Microsoft AI 활용 교육 5줄. 제목이 "…개최 안내" /
  "…개최 안내 및 홍보" / 「」 유무로 달라 지문이 갈린다. 제안: 병합 지문에서 「」·"안내·홍보·개최" 같은
  꼬리말과 괄호를 떼고 견준다(포스터 중복은 그대로 둔다는 결정과 충돌하지 않는다).
- **"내 공지" 이름 + 로그인 없음.** 설정이 기기별 저장이라는 안내가 화면에 없다. 온보딩 문구는
  "선택한 곳의 공지가 '내 공지'에 모입니다"라고만 한다. 제안: 내 설정 상자에 "이 브라우저에만 저장" 한 줄.
- **온보딩.** 커밋 `51eb1b3`(9월 7일) "Let people check organizations instead of walking a setup wizard".
  그런데 첫 진입에 캠퍼스→단과대→학과 3단계가 여전히 뜬다(건너뛰기 있음). 결정을 되살릴지, 온보딩을
  "캠퍼스 하나만 묻기"로 줄일지 판단 필요.
- **주제 "기타"** 가 학사 글을 삼킨다. "[일반대학원] 학점인정 시행", "[학부] 물류관리 증원 안내",
  "FAST 미래교수자 육성" 이 기타. 규칙 사전에 "증원·학점·수강·이수·졸업사정" 등이 빠져 있다.
- **"최대 3시간 지연" 고지** 는 바닥글에만 있다. 목록 머리에 "12:31 기준" 같은 갱신 시각이 없어
  학생은 얼마나 낡은지 모른다.
- **대상 표시.** 상세의 "대상: 건축공학과"는 총무관리처 전교 공지에도 붙는다(게시판 소속을 대상으로
  씀). 학생이 "우리 과만 해당"으로 오해할 수 있다.

## 3. 없는 것 — 통상 기준

| 중요도 | 항목 | 본 것 |
| --- | --- | --- |
| 상 | 마감일·마감 임박 정렬 | 목록·상세 모두 마감 없음(모델엔 `deadline_date` 있음) |
| 상 | 필터·검색의 주소 반영 | `/?q=장학` 으로 열어도 검색 안 됨. 헤더 돋보기는 `?focus=q` 만 |
| 상 | 원문 보기 위치 | 본문 전체 아래 "출처 1곳 → 원문 보기" |
| 상 | 빈 결과의 이유 | "표시할 공지가 없습니다" 한 줄 |
| 중 | 공유·링크 복사 | 없음 |
| 중 | 목록에서 첨부파일 유무 | 없음(상세엔 있음) |
| 중 | 기간 필터(이번 주·이번 달) | 없음 |
| 중 | 읽음 표시·N 기준 | N 이 어떤 기준(며칠)인지 표시 없음 |
| 중 | 마지막 갱신 시각 | 없음 |
| 중 | 출처 탭 용어 | "초기 수집 전 · 연결·검증 대기 · 폐쇄", 폐쇄된 게시판도 나열 |
| 중 | 출처 유형 필터 | 인스타그램·기관 제공 자료는 출처 0개인데 선택 가능 |
| 중 | 연락처 첫 로딩 | 6초 넘게 "불러오는 중…" |
| 하 | 상단으로, 다크 모드, 글자 크기, 키보드 포커스 표시 | 없음 |

## 4. 잘 된 것

375px 가로 넘침 없음. 로그인 요구 없음. 본문 HTML 정리(XSS). 상세의 "원문 등록일 미확정"·
"로그인 필요" 안내. 조직 트리(단과대 ㄱㄴㄷ → 부서 → 기타). 별칭 접기. 주제 배지 색 체계.

## 5. 오늘 확인된 버그(수정 중·완료)

- 내 공지 "공과대학 외 3곳"/칩 2개/외국어대학 글 섞임 — 원인은 옛 설정값(`college_id`·`department_id`)이
  목록 필터에만 더해지던 `mine/page.tsx` 한 줄. 메인 세션의 "중복 병합 대표 출처" 진단은 틀렸다.
  수정됨(배포 대기). 학과 체크 시 단과대 자동 해제(좁히기) 규칙 추가.
- 첫 화면 로딩 9.1초(색인 12조각 + 상세 20건 개별 요청) — 수정 중.
- 온보딩 단과대 목록이 하위 학과를 합산하지 않아 "준비 중" 표시 — 미수정.

## 6. 확인 못 한 것

- 검색 결과 화면(주소로 못 열어 UI 를 눌러야 함) — 결과 수·강조 여부 미확인.
- 인스타그램 출처 경로 — 출처 0개라 화면 확인 불가.
- 실제 학생 기기(iOS Safari) — 미확인.

---

# 모바일 점검 — 2026-09-08 13:55~14:40 KST (375×812)

실제 조회 서버(`https://api.xn--289a5d743g7dm.info`, 개정 `r1788839641-manual`)를 붙이고
375×812 로 직접 눌러 보며 잰 값이다. 밝은 판·어두운 판을 모두 봤다. 데스크톱 1280×720 은
비교용으로만 봤다. **모바일에서만 드러나는 문제**를 적었다.

| 중요도 | 항목 | 근거(실측) | 제안 |
| --- | --- | --- | --- |
| 상 | 첫 화면에서 공지 첫 줄이 화면의 43% 아래에 있다 | 전체 공지 `/`: 헤더 99 + 조직 상자 46 + 검색 40 + 출처 유형 41 + 주제 칩 50 → 첫 공지 줄 **352px**(812의 43%). 내 공지 `/mine` 은 검색·출처 유형이 없어 **256px** | 전체 공지에서도 검색·출처 유형을 헤더 돋보기 뒤로 접고, 조직 상자는 칩 줄 옆 작은 단추 하나로 줄인다 |
| 상 | 상세의 원문 보기가 본문 아래 한 번뿐이라 본문이 있는 공지는 1.2~1.5화면을 내려야 닿는다 | 본문 있는 3건 실측 **977px·1205px**(화면 812), 본문 없는 3건은 429~546px | 상단 출처 줄 오른쪽에 작은 "원문" 링크를 하나 더 둔다 |
| 중 | 조직 고르기 상자가 본문을 511px 밀어내고, 안쪽 목록(422px)에 2,578px 이 들어가 스크롤이 두 겹이 된다 | 열면 첫 공지 줄이 256 → **768px**. 안쪽 목록 `max-h-52vh`, 내용 2,578px. 닫기는 상자 맨 위에만 있어 목록을 내리면 화면 밖으로 나간다 | 모바일에서는 접힘 상자 대신 바닥 시트로 띄우고 머리줄(닫기)을 고정한다 |
| 중 | 주제 칩 줄에 "옆으로 더 있다"는 표시가 잘린 칩뿐이다 | 칩 줄 폭 343px 에 내용 799px. 11개 중 5.5개만 보인다. 화살표·그라데이션 없음 | 오른쪽 끝에 8px 페이드를 얹는다(색 토큰만 씀) |
| 중 | 광고가 목록 5번째 줄 뒤 66px 를 차지한다. 첫 화면에서는 안 보이지만 한 번 내리면 바로 나온다 | 광고 상단 733px, 높이 66px | 그대로 두되 목록 첫 화면(5줄) 밖으로 밀리는지 계속 지켜본다 |
| 중 | 검색 입력 줄이 40px 라 44px 에 못 미친다 | `py-[9px]` + 20px 입력 = 40px | `py-[11px]` 로 4px 만 키운다 |
| 하 | 제목이 늘 2줄 자리를 차지해 한 줄짜리 제목도 카드 높이가 같다 | 카드 높이 92px 고정(첫 카드만 108px), 제목 칸 42px = 14.5px×1.45×2 | 그대로 두는 편이 훑기에 낫다. 바꾸지 않기를 권함 |
| 하 | 포스터 없는 공지와 있는 공지가 섞여 제목 시작 x 좌표가 들쭉날쭉하다 | 포스터 52×52 + 10px, 없으면 0 | 그대로 둔다(포스터가 곧 내용인 공지가 있다) |
| 하 | 출처 이름에 원문 HTML 엔티티가 그대로 남는다 | `4단계 BK21 대학원혁신 Event &amp; Review` 등 3건 | 백엔드에서 이름을 한 번 더 푼다 |

## 이번에 같이 고친 것 (작은 것만)

- 헤더 돋보기·사람 아이콘: 34×34 → 누르는 자리 **44×44**(모양은 그대로, `before:`).
- 주제 칩: 31px → 38px, 줄 높이 41 → 50px, 누르는 자리 44px. 대신 첫 공지 줄이 341 → 352px 로 11px 내려갔다.
- 출처 화면 구독 스위치: 20px → 누르는 자리 44px.
- 캠퍼스 토글은 이미 44px 였다(그대로).

## 문제 없었던 것

- 가로 넘침 없음(`scrollWidth` 375 = `clientWidth` 375).
- 뒤로 가기 스크롤 복원: 1,000px 에서 상세로 들어갔다 돌아와도 1,000px. "더 보기"로 40건까지
  늘린 뒤에도 40건·2,500px 그대로 돌아온다.
- 상단으로 가기 단추(44×44, 오른쪽 12px·아래 16px)는 "더 보기"(y 614~658)·광고와 겹치지 않고,
  바닥글 화면 조절(x 44~208)과도 안 겹친다. Next 개발 표시가 왼쪽 아래라 그것과도 안 겹친다.

## 확인 못 한 것

- 실제 iOS Safari·안드로이드 크롬(에뮬레이션만 씀). 특히 `env(safe-area-inset-bottom)` 은 실기기에서만 값이 생긴다.
- 손가락 스크롤 관성·중첩 스크롤 체감(마우스 휠로만 확인).
- `delayed`·`blocked`·`paused` 게시판 화면 — 지금 데이터에 하나도 없다(active 451 · pending 47 · retired 16).
- 인스타그램 출처 화면 — 게시판 514개가 전부 웹이다.
