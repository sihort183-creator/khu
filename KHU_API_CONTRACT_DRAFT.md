# 공지·연락처 화면 연동 규격 초안

작성일: 2026-09-06. 상태: 프론트의 mock(가상 데이터) 제작용 초안. 실제 서버가 구현된 명세가 아니다.

[백엔드 계획](KHU_BACKEND_PLAN.md)의 0단계에서 이 문서를 OpenAPI(연동 명세)와 검증 가능한 예제 파일로 변환한다. 프론트는 화면을 자유롭게 만들되 아래의 데이터 의미·정보 없음·목록 순서·오류 상태를 함께 표현한다. 실제 필드 계약이 확정되면 가상 자료도 같은 명세로 검사한다.

## 1. 화면이 구분해야 하는 데이터

| 분류 | 의미 | 기본 기능 |
|---|---|---|
| 공지 | 특정 시점에 게시된 안내, 여러 원본을 하나로 묶을 수 있음 | 목록·상세·검색·대상·원문 출처 |
| 연락처 | 기관의 업무별 상시 연락 수단 | 조직·업무 검색·원문 근거·최근 확인·전화·메일 연결 |
| 출처 | 실제 공지·연락처를 제공하는 게시판·페이지·계정 | 소속 조직·수집 범위·연결 상태 |
| 조직 | 대학·캠퍼스·단과대·학과·센터 등 | 소속 탐색·개인화·연락처 연결 |

연락처는 공지 주제 목록의 '학사·장학·행사'와 구분된 정보 분류다. 공지에 전화가 들어 있다는 이유로 연락처 목록으로 이동시키지 않는다. 연락처 조회에 게시일·마감일을 필수로 요구하지 않는다.

## 2. 공통 형식과 값

| 필드·표기 | 설명 |
|---|---|
| `id`(식별자) | 문자열, 의미를 파싱하지 않음. 명칭 변경 후에도 유지 |
| `code`(기계용 코드), `label`(한국어 이름) | 필터·분류·상태는 코드로 처리하고 사용자에게 한국어 이름 표시 |
| `null`(정보 없음) | 원문에 없거나 확정할 수 없음. 0·오늘 날짜·빈 문자열로 대체 금지 |
| `[]`(빈 목록) | 해당 항목이 없음 |
| 날짜 | `YYYY-MM-DD`(연-월-일). 날짜만 있는 자료에 시각 표시 금지 |
| 시각 | 시간대가 포함된 표준 문자열. 예: `2026-09-06T01:00:00Z`(한국 시간 9월 6일 오전 10시) |
| `data`(결과), `meta`(부가정보), `page`(목록 이동) | 성공 응답 공통 묶음. 상세에는 목록 이동 묶음 없음 |
| `request_id`(요청 식별자), `generated_at`(응답 생성 시각) | 오류 문의·최신성 확인용. 화면에 항상 표시할 필요는 없음 |
| `next_cursor`(다음 조회 위치), `has_next`(다음 목록 여부) | 다음 페이지 요청에 사용. 위치 문자열을 프론트에서 해석하지 않음 |
| `snapshot_at`(목록 기준 시각), `dataset_revision`(기존 데이터 개정 번호) | 이어보기의 기준. 새 글 추가와 기존 글 수정의 영향을 구분 |

목록 기본 20개·최대 100개. 전체 건수는 필수 응답이 아니므로 화면에 전체 페이지 수를 반드시 요구하지 않는다. 빈 결과도 정상 성공이다. 동일 카드가 여러 출처에서 왔더라도 공지 식별자로 한 번만 표시한다.

## 3. 분류 사전과 조회 조건

`GET /v1/catalog`(분류 사전 조회)는 캠퍼스·조직 유형·공지 주제·매체·기능 활성 여부를 반환한다. 코드와 표시 명칭을 분리한다. 백엔드와 프론트가 서로 다른 카테고리 목록을 수동 관리하지 않는다.

공지 주제의 코드 초안은 `academic`(학사), `graduation`(졸업), `scholarship`(장학), `career`(취업·인턴), `startup`(창업), `program`(프로그램·공모전), `international`(국제), `event`(행사), `student_council`(학생회), `campus_life`(생활), `other`(기타)다. 연락처는 별도 정보 종류이므로 이 목록에 넣지 않는다.

| 조회 조건 | 적용 | 의미 |
|---|---|---|
| `q`(검색어) | 공지·연락처·출처 | 최대 100자 |
| `campus_id`(캠퍼스 식별자) | 공지·연락처·조직·출처 | 한 종류 안에서 복수 값은 합집합 |
| `organization_id`(조직 식별자) | 공지·연락처·출처 | 아래 조직 포함 여부는 별도 조건으로 지정 |
| `include_descendants`(하위 조직 포함) | 조직 기반 조회 | 기본 거짓. 내 공지의 상속 규칙과는 별개 |
| `category_code`(공지 주제 코드) | 공지 | 학사·졸업·장학·취업인턴·창업·프로그램공모전·국제·행사·학생회·생활·기타 |
| `source_id`(출처 식별자) | 공지·연락처 | 원본을 제공한 출처 기준 |
| `medium`(매체) | 공지·출처 | `web`(웹), `instagram`(인스타그램), `submitted`(기관 제공 자료) |
| `published_from`(게시 시작일), `published_to`(게시 종료일) | 공지 | 원문 등록일 기준, 양 끝 날짜 포함. 날짜 미확정은 이 필터 결과에서 제외 |
| `sort`(정렬 방식) | 목록 | 허용된 값만 사용 |
| `limit`(요청 개수), `cursor`(다음 조회 위치) | 목록 | 이어보기는 첫 요청의 필터·정렬과 동일해야 함 |

공지 정렬 값은 `recent`(서비스에 새로 들어온 순), `published`(원문 게시일순), `relevance`(검색 관련도순)다. 연락처는 기본 `name`(기관·업무 이름순), 검색 시 관련도순이다. 날짜·점수·이름이 같으면 안정 식별자를 마지막 기준으로 쓴다.

공지의 캠퍼스·조직 필터는 공지 대상 범위에 적용한다. 출처 화면의 캠퍼스·조직 필터는 출처 소유 조직에 적용한다. 한 필터 안의 값은 합집합, 서로 다른 필터는 교집합으로 처리한다. 복수 값의 질의 문자열 표기는 명세에서 반복 매개변수 방식으로 통일한다.

## 4. 공지 목록·상세 모델

| 필드 | 의미 |
|---|---|
| `kind`(정보 종류) | `notice`(공지) |
| `title`(제목), `excerpt`(본문 일부) | 요약 생성문이 아닌 원문 기반 제목·본문 일부 |
| `primary_category`(대표 주제), `secondary_categories`(보조 주제) | 코드·한국어 이름 묶음 |
| `audiences`(대상 범위) | 대학 전체·캠퍼스·조직·미확정, 각각의 유형·식별자·이름 |
| `audience_note`(대상 안내) | 명시적 제한·미확정 설명. 신청 자격을 확정하는 기능은 아님 |
| `primary_source`(대표 출처) | 출처 식별자·이름·매체 |
| `source_count`(원문 출처 개수) | 현재 연결된 공개 원본 수 |
| `published_date`(원문 등록일), `published_at`(정확한 원문 등록 시각) | 정확한 시각을 모르면 후자는 정보 없음 |
| `published_precision`(등록일 정밀도) | `date`(날짜), `datetime`(시각 포함), `unknown`(미확정) |
| `first_visible_at`(최초 노출 시각) | 기본 정렬 기준. 재수집·연락처 갱신으로 바뀌지 않음 |
| `updated_at`(내용 갱신 시각) | 서비스가 의미 있는 내용 변경을 반영한 시각 |
| `deadline`(마감 정보) | 미확정이면 정보 없음. 있으면 날짜·선택적 시각·정밀도·원문 근거 |
| `original_url`(대표 원문 주소) | 사용자가 원문으로 이동할 주소 |
| `original_status`(원문 상태) | `available`(접근 가능), `unavailable`(확인 불가), `removed`(삭제 확인) |
| `freshness`(갱신 상태) | 상태 코드·한국어 이름·마지막 확인 시각 |

다음은 실제 공지와 무관한 가상 응답이다. 예시 도메인과 가상 식별자는 운영에 가져오지 않는다. 중첩 객체의 `name`은 이름, `type`은 유형, `last_checked_at`은 마지막 정상 확인 시각을 뜻한다.

```json
{
  "data": [
    {
      "id": "demo-notice-001",
      "kind": "notice",
      "title": "[가상 자료] 2학기 장학 신청 안내",
      "excerpt": "화면 구성을 확인하기 위한 가상 본문입니다.",
      "primary_category": {"code": "scholarship", "label": "장학"},
      "secondary_categories": [],
      "audiences": [
        {"type": "campus", "id": "demo-campus-seoul", "name": "서울캠퍼스"}
      ],
      "audience_note": null,
      "primary_source": {
        "id": "demo-source-001",
        "name": "가상 장학 안내 게시판",
        "medium": {"code": "web", "label": "웹"}
      },
      "source_count": 2,
      "published_date": "2026-09-03",
      "published_at": null,
      "published_precision": "date",
      "first_visible_at": "2026-09-03T01:00:00Z",
      "updated_at": "2026-09-03T01:00:00Z",
      "deadline": null,
      "original_url": "https://example.org/notices/demo-001",
      "original_status": {"code": "available", "label": "접근 가능"},
      "freshness": {
        "code": "fresh",
        "label": "최근 확인됨",
        "last_checked_at": "2026-09-06T00:45:00Z"
      }
    }
  ],
  "page": {
    "next_cursor": null,
    "has_next": false,
    "snapshot_at": "2026-09-06T01:00:00Z",
    "dataset_revision": "demo-notice-revision-1"
  },
  "meta": {
    "request_id": "demo-request-001",
    "generated_at": "2026-09-06T01:00:00Z"
  }
}
```

상세 응답은 공지 공통 필드에 다음을 더한다. 같은 공지의 목록·상세 필드 의미가 달라지지 않아야 한다.

| 상세 필드 | 의미 |
|---|---|
| `body_text`(일반 텍스트 본문), `body_html`(안전하게 정리한 웹 본문) | 프론트는 일반 텍스트 또는 정리된 본문 중 적절한 표시 방식을 선택 |
| `sources`(전체 원문 출처) | 원본 식별자·출처 이름·주소·등록일·원문 상태·대표 여부 |
| `attachments`(첨부) | 식별자·파일명·종류·크기·원문 주소·확인 상태. 다운로드 불가도 표현 |
| `contact_mentions`(이 공지의 문의처) | 문의처 원문·연락 수단·선택적 상시 연락처 식별자 |
| `related_notices`(연결된 공지) | 분리·후속 공지 등의 식별자·제목·관계 이름 |
| `resolved_from_id`(이전 공지 식별자) | 병합 전 주소로 들어왔을 때 원래 식별자, 평소에는 정보 없음 |

문의처를 누르면 전화·메일 연결이 가능할 수 있지만 자동 발신·자동 발송은 하지 않는다. 실제 신청·자격·최종 내용은 원문으로 확인할 수 있어야 한다.

## 5. 연락처 목록·상세 모델

| 필드 | 의미 |
|---|---|
| `kind`(정보 종류) | `contact`(연락처) |
| `organization`(소속 조직) | 식별자·정식 이름·조직 경로 |
| `campuses`(해당 캠퍼스) | 공통 기관이면 여러 캠퍼스 가능 |
| `service_name`(담당 업무) | 대표 안내·학적·장학·시설 등 |
| `channels`(연락 수단 목록) | 전화·이메일·팩스·공식 링크를 배열로 제공 |
| `channel.kind`(수단 종류) | `phone`(전화), `email`(이메일), `fax`(팩스), `website`(공식 링크) |
| `display_value`(원문 표기), `value`(정규화 값) | 원문 표기는 보존, 정규화 불확실하면 정규화 값만 정보 없음 |
| `action_url`(연결 주소), `extension`(내선) | 안전하게 확정할 수 있을 때만 제공. 팩스에는 자동 전화 연결 주소 없음 |
| `location`(사무실 위치), `office_hours`(업무시간) | 원문에 있는 내용만 제공 |
| `official_url`(공식 홈페이지) | 기관 업무 안내 주소 |
| `verification`(검증 상태) | 상태·최근 원문 일치 확인·안내 문구 |
| `verified_at`(값을 검증한 시각), `message`(한국어 안내) | 전화 통화·메일 전달 검증과는 다름 |
| `evidence`(표시값의 근거) | 필드 경로·원문 주소·출처 이름·관찰 시각 |
| `field`(대상 필드), `url`(근거 주소), `source_name`(근거 출처명), `observed_at`(원문 관찰 시각) | 근거 항목의 구성 |

검증 상태는 `verified`(원문 확인됨), `stale`(확인 오래됨), `conflict`(내용 충돌 검토 중)이다. `hidden`(비공개)·`retired`(폐기)는 기본 공개 목록에 포함하지 않으며 상세에서 이용 불가 상태로 처리한다.

아래는 예시용 이메일을 사용한 가상 상세 응답이다. 실제 학교 연락처가 아니다.

```json
{
  "data": {
    "id": "demo-contact-001",
    "kind": "contact",
    "organization": {
      "id": "demo-organization-001",
      "name": "가상 학과 행정실",
      "path": ["경희대학교", "서울캠퍼스", "가상 학과"]
    },
    "campuses": [{"id": "demo-campus-seoul", "name": "서울캠퍼스"}],
    "service_name": "학적 문의",
    "channels": [
      {
        "id": "demo-channel-001",
        "kind": {"code": "email", "label": "이메일"},
        "display_value": "office@example.org",
        "value": "office@example.org",
        "action_url": "mailto:office@example.org",
        "extension": null
      }
    ],
    "location": null,
    "office_hours": null,
    "official_url": "https://example.org/department",
    "verification": {
      "code": "verified",
      "label": "원문 확인됨",
      "verified_at": "2026-09-05T03:00:00Z",
      "message": "공식 원문의 안내와 일치함을 확인한 시각입니다."
    },
    "evidence": [
      {
        "field": "channels.demo-channel-001",
        "url": "https://example.org/department/contact",
        "source_name": "가상 학과 연락처 안내",
        "observed_at": "2026-09-05T03:00:00Z"
      }
    ]
  },
  "meta": {
    "request_id": "demo-request-002",
    "generated_at": "2026-09-06T01:00:00Z"
  }
}
```

`path`(조직 경로)는 표시용이며 프론트가 문자열 비교로 조직 관계를 계산하지 않는다. 상세의 근거는 실제로 표시한 모든 필드에 연결하며, 가상 예시의 연락처 수단 근거는 구조 설명용 최소 사례다. 목록은 근거 상세를 제외할 수 있으나 대표 근거 주소와 검증 상태는 반환한다.

## 6. 출처·조직 상태

출처 모델에는 식별자·이름·소속 조직·매체·콘텐츠 종류·원문 주소·수집 상태·최근 성공·초기 수집 범위를 담는다. 연락처 안내 출처도 같은 목록에서 종류를 구분할 수 있다.

출처 상태 코드는 `active`(수집 중), `delayed`(갱신 지연), `blocked`(접근 제한), `pending`(연결·검증 대기), `paused`(운영 중지), `retired`(폐쇄)다. 운영 내부의 자세한 오류는 이 코드와 한국어 안내로 변환한다. 아직 연결되지 않은 인스타그램 계정을 정상 수집 중으로 표시하지 않는다.

조직 목록은 상위 조직 조건과 하위 존재 여부를 제공한다. 학과의 캠퍼스·단과대 선택 조합이 유효한지는 서버에서 검사한다. 캠퍼스·조직 종류를 프론트 코드에 고정된 두 가지 값으로 제한하지 않는다.

## 7. 비회원 맞춤 조회·계정 저장

`POST /v1/feeds/preview`(비회원 맞춤 조회)는 `campus_id`(선택 캠퍼스), `organization_ids`(소속 단과대·학과), `subscribed_source_ids`(추가 관심 출처), `filters`(추가 조회 조건), `cursor`(다음 위치), `limit`(개수)을 받는다. 실제 존재하는 조직 관계를 검사하고 구독 출처는 최대 100개로 시작한다. 응답은 공지 목록과 같다.

기기 저장값은 비회원의 유일한 설정 사본이다. 이 요청은 공개 자료 조회이고 서버에 개인 프로필을 만들지 않는다. 출처 번호 목록을 로그에 그대로 쌓지 않는다. 프론트가 바꾼 조건과 이전 이어보기 위치가 섞이면 오류 후 첫 페이지를 요청한다.

계정 기능이 활성화되면 같은 설정 의미를 계정 조회에도 적용한다. `GET /v1/me/profile`(계정 설정 조회) 응답에 `version`(설정 버전)을 포함하고, `PATCH /v1/me/profile`(계정 설정 변경)는 같은 버전을 보내 충돌을 검사한다. 계정 설정은 소속·관심 출처·저장한 공지를 구분해 관리한다.

로그인 제공자 화면·토큰 갱신은 인증 연동을 사용하고 백엔드는 토큰을 검증한다. 가상 서버가 개발용 고정 토큰을 사용할 경우 실제 검증·운영 서버에서는 해당 경로를 완전히 비활성화한다.

## 8. 오류와 화면 상태

| 응답 상태·코드 | 한국어 의미 | 프론트 동작 |
|---|---|---|
| 200 + 빈 결과 | 조건에 맞는 항목 없음 | 빈 상태·조건 초기화 표시 |
| 400 `INVALID_REQUEST`(잘못된 요청) | 조회 형식·위치 문자열 오류 | 올바른 조건으로 수정 |
| 422 `INVALID_FILTER`(조회 조건 오류) | 존재하지 않는 소속·잘못된 날짜 등 | 관련 입력 항목 안내 |
| 401 `AUTH_REQUIRED`(로그인 필요) | 계정 기능의 인증 없음·만료 | 로그인·토큰 갱신 동작 |
| 403 `FORBIDDEN`(권한 없음) | 사용자·운영 권한 부족 | 접근 제한 표시 |
| 404 `NOT_FOUND`(대상 없음) | 식별자에 해당하는 공개 대상 없음 | 목록으로 이동 가능 |
| 409 `FEED_CHANGED`(목록 갱신 필요) | 수정·병합 등으로 이어보기 기준 변경·만료 | 기존 이어보기 폐기, 첫 페이지 재조회 |
| 409 `VERSION_CONFLICT`(설정 충돌) | 다른 기기·운영자가 먼저 수정함 | 최신값을 받아 재적용 |
| 410 `CONTENT_UNAVAILABLE`(자료 이용 불가) | 서비스에서 공개 중단됨 | 민감한 비공개 사유 없이 안내 |
| 429 `RATE_LIMITED`(요청량 제한) | 잠시 요청을 줄여야 함 | 서버의 재시도 간격 준수 |
| 503 `TEMPORARILY_UNAVAILABLE`(일시 이용 불가) | 조회 저장소 등 필수 기능 장애 | 오류 상태와 재시도 제공 |

오류의 `error`(오류 묶음)에는 `code`(코드), `message`(한국어 안내), `retryable`(재시도 가능), `details`(입력 필드 오류 목록), `request_id`(요청 식별자)를 담는다. 내부 오류 추적문·쿼리·토큰은 포함하지 않는다.

원문 사이트가 잠시 실패했지만 저장된 공지가 존재하면 공지 조회 자체는 정상 응답이고 원문·갱신 상태만 바뀐다. 서비스 데이터베이스가 실패한 경우를 공지 0건으로 표시하지 않는다.

오류 신고는 접수 완료를 202(처리 접수)로 반환하고 즉시 내용이 고쳐졌다고 표시하지 않는다. 일반 신고자가 다른 사람의 신고 상세를 조회하는 공개 경로는 제공하지 않는다.

## 9. 가상 데이터 필수 시나리오와 인계

1. 공지 정상 1건·다중 출처 1건·본문 표·첨부만 있는 글·이미지 위주 글.
2. 등록일만 있고 시각 없음·마감 미확정·원문만 확인 불가.
3. 전체 공지 빈 결과·검색 결과·두 페이지 이어보기·목록 개정으로 재조회.
4. 연락처 정상·전화 여러 개·내선만 있음·팩스·위치 없음·업무시간 없음.
5. 연락처의 확인 오래됨·값 충돌·비공개·폐기.
6. 출처 수집 지연·연결 대기인 인스타그램·초기 90일만 수집된 출처.
7. 비회원 맞춤 설정·설정 미선택·유효하지 않은 캠퍼스와 학과 조합.
8. 계정 기능 비활성·로그인 만료·설정 버전 충돌·북마크 병합 후 주소 변경.
9. 느린 요청·요청량 제한·일시 장애·오류 신고 접수.

기계 판독형 명세를 정본으로 삼은 뒤 예제·가상 서버·실제 서버가 같은 검사에 통과해야 한다. 백엔드가 프론트 구현을 대신 수정하지 않아도, 프론트가 가상 데이터 공급 부분을 실제 주소로 바꾸면 같은 화면 상태를 재현할 수 있는 것을 인계 기준으로 삼는다.
